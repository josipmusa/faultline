# Faultline — Build Roadmap

This roadmap breaks the product described in SPEC.md into stages, and each stage into small tasks. It is written for coding agents working one task at a time with a human verifying gates.

## How to use this document

- Work **one task at a time**, in order, unless a task says it is independent.
- Every task has a **Verify** block. `Agent` means the agent proves it with tests or commands and reports the output. `Manual` means the human runs the steps and confirms what they see. Do not start the next task until the current one is verified.
- Every stage ends with a **Stage gate**, always manual. Do not start the next stage until the gate passes.
- When a task is done, tick its checkbox here and note anything surprising under it in one line. Never rewrite a task's scope silently; propose changes in the conversation.
- Fixed decisions live in AGENTS.md. If a task seems to conflict with one, stop and ask.

Conventions used below:

- Admin address is `http://localhost:9000` (UI, HTTP API, WebSocket, MCP over HTTP).
- Forward proxy listens on `localhost:9001`.
- Explicit routes get their own ports starting at `9100`.
- Config file is `faultline.yaml` in the working directory. Per-user state (CA, truststores) lives in the OS config directory, `~/.config/faultline` on Linux.

---

## Stage 0 — Project skeleton

Goal: a buildable, testable, lintable Go module with a CLI that does nothing useful yet.

- [x] **0.1 Go module and CLI skeleton.** Create `go.mod` (module `github.com/josipmusa/faultline`, Go 1.25), `cmd/faultline/main.go` using Cobra with subcommands `serve`, `run`, `rule`, `scenario`, `ca`, `mcp`, `version`, each printing "not implemented" for now. Add a `Makefile` with `build`, `test`, `lint`, `run` targets.
  - Verify (Agent): `make build` produces `./bin/faultline`; `./bin/faultline --help` lists the subcommands; `./bin/faultline version` prints a version string.
  - Note: `make` is not installed on the dev machine, so the targets were verified by running the commands they wrap. Version is injected with `-ldflags -X main.version`.
- [x] **0.2 Lint and test tooling.** Add `golangci-lint` config with a sensible default set, a `go vet` step, and one trivial test so the pipeline has something to run.
  - Verify (Agent): `make lint` and `make test` both pass and exit 0.
  - Note: golangci-lint v2 config schema (`version: "2"`), pinned to v2.13.2 and installed by a new `make tools` target. `-race` needs a C compiler, absent here, so tests were verified without it.
- [x] **0.3 CI workflow.** GitHub Actions workflow running lint, test, and build on push and pull request, Linux and macOS.
  - Verify (Agent): workflow YAML validates with `actionlint` if available, otherwise by careful review; the human will see it run after the first push.
  - Note: `actionlint` exits clean. CI runs `make tools`/`lint`/`test`/`build` so the Makefile stays the single source of truth.
- [x] **0.4 Repository documents.** `README.md` stub with the one-paragraph pitch from SPEC.md and a "status: pre-alpha" banner. `LICENSE` (MIT). `CONTRIBUTING.md` stub pointing to AGENTS.md.
  - Verify (Agent): files exist and README renders sanely.
  - Note: LICENSE copyright reads "2026 Josip Musa" — correct it if that is not the right holder.

**Stage 0 gate (Manual).** Clone into a fresh directory, run `make build && ./bin/faultline --help`. Expect the help text with all subcommands and no warnings.

---

## Stage 1 — Core engine and explicit routes

Goal: traffic flows through Faultline in the simplest attach mode, faults can be applied, and everything observed is recorded. No UI yet; the HTTP API is the interface.

- [x] **1.1 Rule model.** Define the domain in `internal/rules`: `Rule` with an id, name, enabled flag, `Match` (upstream host, optional method, optional path glob, optional header equals), and a single `Fault`. Faults for this stage: `delay` (ms, optional jitter ms) and `status` (code, optional body). Values are immutable; updates return new copies. Include JSON tags matching the API shape in AGENTS.md.
  - Verify (Agent): unit tests for matching cover host only, host plus method, glob patterns like `/orders/*` and `/v1/**`, header match, and disabled rules never matching.
  - Note: no `behavior` field yet (Stage 1 is one fault per rule) and no `Validate()` — validation belongs at the API boundary in 1.6. Empty match fields, `host` included, match anything. `-race` still needs a C compiler that is absent here, so tests ran without it.
- [x] **1.2 Rule store.** An in-memory, concurrency-safe store with list, get, add, update, delete, enable, disable. Emits a change notification channel that later stages will use for hot reload and UI refresh.
  - Verify (Agent): unit tests including a concurrent add and read test run with `-race`.
  - Note: slice-backed so insertion order is preserved, which is the order 1.4 will consult rules in. One coalescing `Changes()` channel (buffered 1, non-blocking send), so a reader must re-read the store rather than count signals; the admin server fans out to multiple websocket clients in 1.7. No id generation — 1.6 must mint an id for `POST /api/rules`, whose example body carries none. The concurrent test could not run under `-race` here (no C compiler on this machine); CI runs `make test` with `-race` on Linux and macOS.
- [x] **1.3 Event recording.** `internal/events`: an `Event` per proxied request with id, timestamp, upstream host, method, path, status, duration, bytes in and out, `faulted` flag, applied rule id, and a `tier` field (`plain`, `intercepted`, `encrypted`) telling how much Faultline could see. A ring buffer with configurable size, default 1000, plus a fan-out subscriber mechanism. Port the good parts of the previous projects' event manager.
  - Verify (Agent): unit tests for ring buffer wraparound, subscriber fan-out, and slow-subscriber drop behavior under `-race`.
  - Note: ported the ring arithmetic and the buffered-channel fan-out from both predecessors, dropped the `SourceServiceId` partitioning, and fixed two bugs in them — `NewEventBuffer(0)` divided by zero on first add, and `Close()` never cleared the listener slice so a second call closed a closed channel. `Subscribe()` returns a `*Subscription` that closes itself rather than making the caller hand the channel back. The ring buffer is the source of truth and the stream is best effort: a subscriber behind by more than 100 events loses them instead of blocking the request path. No `Clear()` (1.6 needs it for `DELETE /api/events`), no filtering (1.6), and no event id generation — 1.4 must mint ids. `duration_ms` is a whole number, so sub-millisecond round trips record as 0. `-race` still unavailable locally (no C compiler); CI covers it.
- [x] **1.4 Fault pipeline.** `internal/faults`: a `RoundTripper` wrapper that looks up the matching rule for a request, applies the fault, and records an event whether faulted or not. Delay must respect request context cancellation (no bare `time.Sleep`). Status fault short-circuits with a synthetic response and a `Faultline-Fault: <rule id>` header so users can tell a synthetic response from a real one. Start from `ChaosTransport` in `../resilience-proxy/internal/proxy/interceptor.go`.
  - Verify (Agent): unit tests using `httptest.Server` as upstream: no rule passes traffic unchanged; delay rule adds at least the configured delay; delay is cancelled promptly when the request context is cancelled; status rule returns the configured code and header without hitting upstream.
  - Note: `ChaosTransport` read the matched rule out of a context value and slept with a bare `time.Sleep`; this version looks the rule up itself from the store and waits on a timer against the request context. Event ids had to be minted here, so `events.NextID()` (atomic counter) was added — the predecessor used `UnixNano`, which collides under concurrency. `bytes_in`/`bytes_out` come from `Content-Length` and are 0 when the length is unknown.
- [x] **1.5 Explicit route listener.** `internal/proxy/reverse`: given an upstream URL and a local port, start a reverse proxy through the fault pipeline. Support multiple routes at once. Preserve the upstream `Host` header semantics correctly and strip hop-by-hop headers. Start from the `httputil.ReverseProxy` director in `../resilience-proxy/internal/proxy/handler.go`.
  - Verify (Manual): run `./bin/faultline serve --route stripe=https://httpbin.org --route-port stripe=9100`; run `curl -i localhost:9100/get`. Expect a 200 JSON response from httpbin with your request echoed back.
  - Note: ran the manual command; httpbin echoed `"Host": "httpbin.org"` and `"url": "https://httpbin.org/get"`, so the Host rewrite is right. Uses `ReverseProxy.Rewrite`/`SetURL` rather than the predecessor's `Director`, which also gets hop-by-hop stripping for free — except `TE: trailers`, which RFC 7230 requires be forwarded and net/http keeps; there is a test pinning that. The verify block invokes `faultline serve --route/--route-port`, so the `serve` placeholder was replaced with a real command; it starts routes only, since the admin server is 1.6, and it errors out if no `--route` is given. Routes without an explicit port are numbered from 9100 upwards, skipping ports already claimed. Signal handling lives in `serve` for now and 1.8 takes it over.
- [x] **1.6 HTTP API for rules and events.** `internal/admin`: JSON endpoints `GET/POST /api/rules`, `GET/PUT/DELETE /api/rules/{id}`, `POST /api/rules/{id}/enable|disable`, `GET /api/events?limit=&host=&faulted=`, `DELETE /api/events`, `GET /api/upstreams` (distinct hosts seen with tier and counts), `GET /api/health`. Validate every input and return structured errors `{ "error": "...", "field": "..." }`.
  - Verify (Agent): handler tests for every endpoint including validation failures. Verify (Manual): with the route from 1.5 running, `curl -X POST localhost:9000/api/rules -d '{"name":"slow","match":{"host":"httpbin.org"},"fault":{"type":"delay","ms":2000}}'`, then `time curl localhost:9100/get`. Expect roughly two seconds. Then `curl localhost:9000/api/events` and expect the request listed with `faulted: true`.
  - Note: ran both verifies; the manual one took 2.60s and the event came back `faulted: true` with `rule_id: slow`. Validation lives in `internal/admin/validate.go`, not on the domain type, as 1.1 decided; it names the field (`fault.ms`, `match.host`). A POSTed rule defaults to `enabled: true` — decoding into a pre-enabled `Rule` distinguishes absent from explicit `false`, so the verify body works without an `enabled` field — and unknown JSON fields are rejected, which will need revisiting when `behavior` arrives in Stage 3. `POST /api/rules` mints an id by slugifying the name and adding `-2`, `-3` on a clash; an explicit id that already exists is a 409. Events come back oldest first and `limit` keeps the most recent N. `/api/upstreams` reports the tier of the host's most recent event, so a host moves from `encrypted` to `intercepted` when interception starts working. `Recorder.Clear()` was added for `DELETE /api/events`; filtering lives in the admin package since it is query shaped. `serve` now starts the admin server on 9000 as well as the routes, which the manual verify needs. `-race` runs on this machine now, so the whole suite is green under it.
- [x] **1.7 Live event stream.** WebSocket endpoint `/api/events/stream` that sends each new event as `{"type":"event","event":{...}}` and a `{"type":"rules_changed"}` message whenever the rule store changes. Read-only; the socket accepts no commands.
  - Verify (Manual): `websocat ws://localhost:9000/api/events/stream` (or `wscat`), then curl the route a few times. Expect one JSON line per request appearing live, and a `rules_changed` line when you add a rule via the API.
  - Note: ran the manual verify with a throwaway Go client (no `websocat` or `wscat` on this machine); two curls produced two event messages, `POST /api/rules` produced `{"type":"rules_changed"}`, and the next curl came back `faulted: true`. Added `github.com/coder/websocket` — Go has no stdlib WebSocket, and it is zero-transitive-dependency and context-native, which suits the "every network op takes a context" convention better than the predecessors' `gorilla/websocket`. **The task text originally said each event goes out as bare JSON; it is a tagged `admin.Message` envelope instead**, because discriminating on the absence of a `type` field breaks silently the day `events.Event` gains one — 2.2 adds CONNECT events — and because the strict-TypeScript UI wants a discriminated union. A new message kind gets a new `type` and its own optional payload field; the envelope is documented in AGENTS.md. No ordering is promised between an event and a notice — they are separate sources feeding one `select`. `ruleWatcher` in `internal/admin/stream.go` does the fan-out 1.2 anticipated, since `Store.Changes()` has a single reader; its per-client signals coalesce the same way the store's do, so a client re-reads `GET /api/rules` rather than counting signals. The handler subscribes *before* the upgrade, so nothing recorded during the handshake is lost. `CloseRead` gives the read-only property for free: any data message from the client closes the socket with `StatusPolicyViolation`. Writes have a 10s timeout so a stalled reader cannot pin a goroutine and its subscriptions. `Server.Shutdown` closes the watcher, which makes every open stream handler send `StatusGoingAway` and return — hijacked connections are invisible to `http.Server.Shutdown`, so nothing else would. It does not *wait* for those goroutines, though, so on a real Ctrl-C the process can exit before the frame is flushed and the client sees a bare EOF; confirmed by hand. Draining them belongs to 1.8.
- [x] **1.8 Graceful shutdown.** Ctrl-C stops accepting new requests, lets in-flight requests finish within a timeout, closes streams cleanly. Port and clean up the previous shutdown code.
  - Verify (Agent): integration test that starts the server, begins a delayed request, sends the shutdown signal, and asserts the request completes and the process exits within the timeout.
  - Note: `TestServeShutsDownGracefullyOnSignal` in `cmd/faultline/shutdown_test.go` is the Verify, and it sends a real `os.Interrupt` to the test process rather than cancelling a context, so the signal wiring is covered too; it holds its own `signal.Notify` for the length of the test so a stray interrupt can never fall through to the default action and kill the test binary. Also ran it by hand: a 3s delay fault in flight when Ctrl-C landed still returned 200 with a full body, shutdown took 2.66s, and the stream client saw `StatusGoingAway`, not the bare EOF 1.7 recorded. Little was portable from the predecessors: their global `IsShuttingDown()` atomic became a `draining` flag on `admin.Server` guarded by the mutex that already exists, and their `wg.Wait()` for in-flight proxy requests was dropped as redundant, since `http.Server.Shutdown` already waits and our handlers do not spawn goroutines; their `log.Fatalf` on a shutdown error went too. What was genuinely missing: `admin.Server.Shutdown` now waits for open stream goroutines (`enterStream`/`leaveStream` around a `WaitGroup`, `waitFor` bounding the wait by the shutdown context), because a hijacked connection is invisible to `http.Server.Shutdown`. A stream opened while draining gets a 503 rather than a socket that is about to close. **The close path needed bounding:** `coder/websocket`'s `CloseRead` goroutine calls `Close`, which waits on a channel that same goroutine only closes on return, so any close wedges for the library's fifteen second timeout once a client has sent a data message — that is now on the shutdown path, so both closes go through `closeBounded` and are abandoned after a second, by which point the frame is already written. `TestShutdownIsPromptAfterAClientCommand` pins it (15s to 2s). `serve` installs the signal handler before anything binds, shuts routes down before the admin server so the last events reach the streams before they close, and prints `shutdown complete`. Its internal signature took an `adminPort` argument, passed `admin.DefaultPort` by the command and 0 by the test, so the test does not fight a real Faultline for port 9000; no new CLI flag.
  - Note (2026-09-09, found via CI): shutting routes down first was necessary but not sufficient, and `TestServeShutsDownGracefullyOnSignal` failed on ubuntu-latest while passing on macOS. The stream handler's `select` had one case for a recorded event and one for the closed watcher, and **`select` chooses at random among ready cases**, so once the handler had not been scheduled since the last request completed, its already-queued events were dropped in favour of the close frame. Rare on an idle laptop, common on a loaded two-core runner under `-race`. `flushQueued` now writes what is queued before the going-away close; a test that queues a full backlog and shuts down delivered only 1 to 5 of 100 events before the fix, and all 100 after, so the loss was much larger than the one missing event CI reported. Reproduced deterministically by sleeping 50ms before the select (6 of 10 runs failed, 0 after).

**Stage 1 gate (Manual).** Start Faultline with two routes to two different upstreams. Add a delay rule for one and a `503` rule for the other via the API. Curl both. Expect the first to be slow with a real body, the second to be an instant 503 with the `Faultline-Fault` header. List events and see both, correctly attributed. Ctrl-C and expect a clean exit message.

  - Passed 2026-09-08, run and confirmed by the human. Upstreams were `https://httpbin.org` on 9100 and `https://postman-echo.com` on 9101. The dry run beforehand gave: 200 with a real 287 byte httpbin body after 2.6s; 503 in 24ms carrying `Faultline-Fault: echo-is-down` and never contacting postman-echo; both events `faulted: true` with the right `rule_id` and `duration_ms` 2585 against 0; `shutting down` then `shutdown complete` on Ctrl-C.

---

## Stage 2 — Forward proxy and encrypted traffic

Goal: applications reach Faultline with standard proxy settings, and HTTPS works in both tiers.

- [x] **2.1 Plain HTTP forward proxy.** `internal/proxy/forward`: accept absolute-URL requests on `:9001`, run them through the same fault pipeline as routes. Port the hop-header handling from the old service. Reject non-absolute requests with a helpful message.
  - Verify (Manual): `curl -x localhost:9001 http://httpbin.org/get`. Expect a normal response and an event in `/api/events` with tier `plain`.
  - Note: ran the manual verify; httpbin echoed the request with no `Proxy-Connection` header and the event came back `tier: plain`, `faulted: false`, 431ms. A delay rule for the host applied through the proxy too, at 1.64s. **The old service's `cleanHopHeaders` was not portable as written** — it deletes `Connection` in the loop before reading it, so the per-connection headers that `Connection` lists were never removed, and it also ran on the inbound `http.Header` rather than on a clone. `httputil.ReverseProxy` with `Rewrite` does the job correctly in both directions, which is also what 1.5 uses, so the forward handler is a `ReverseProxy` whose destination comes from the request line instead of a fixed upstream. `Rewrite` rather than `Director` matters here for a second reason: it means no `X-Forwarded-*` is added, and a forward proxy has no business announcing its client to the upstream. CONNECT answers `501` naming CONNECT rather than falling into the absolute-URL message, since `curl -x` on an https URL would otherwise get told to use an absolute URL it cannot use; 2.2 replaces that branch. **Routes are no longer required:** `serve` starts the forward proxy unconditionally, so `--route` is optional and `parseRoutes` accepts none — HTTP_PROXY alone is reason enough to run Faultline. New `--proxy-port` flag, defaulting to 9001, so tests can bind port 0. `golangci-lint` was not installed on this machine; installed it, and `make lint` is clean.
- [x] **2.2 CONNECT tunnel with connection-tier faults.** Handle `CONNECT host:443`. Without interception, tunnel bytes both ways. Before dialing, consult rules whose match is host only and whose fault is a connection fault: apply delay, or refuse the connection when the fault says so. Record an event with tier `encrypted`, method `CONNECT`, no path.
  - Verify (Manual): `curl -x localhost:9001 https://httpbin.org/get` works. Add a delay rule for `httpbin.org` and repeat; expect the delay. Add a `refuse` fault (introduced in this task as the first connection fault) and expect curl to report the proxy refused the connection. Events show tier `encrypted`.
  - Note: ran the manual verify; the plain tunnel answered 200 in 0.95s, the same request with a 2000ms delay rule took 2.54s, and with a `refuse` rule curl said `(56) CONNECT tunnel failed, response 502`. All four events came back `method: CONNECT`, `path: ""`, `tier: encrypted`. **A refused connection is answered as `502 Bad Gateway` with `Faultline-Fault: <rule id>`** rather than by dropping the socket: that is what a client behind a proxy sees when the real upstream is not listening, and closing the socket would leave nothing to hang the rule id on. The `refuse` fault also works on the plain tier through the transport, as the same synthetic 502, so it never silently no-ops anywhere. Connection-tier matching lives in `faults.Dialer`, the counterpart of `faults.Transport`: a rule applies to a tunnel only if its match is host-only (`Rule.MatchesConnection`) and its fault is a connection fault (`Fault.IsConnection`); a host-only `status` rule is skipped so the rules behind it still get a turn, and the event's tier says why it did not run. The event is recorded when the tunnel opens, so `duration_ms` covers the delay and the dial and `bytes_in`/`bytes_out` stay 0: the bytes inside are opaque and counting them would hold the event back until the tunnel closes. `Dial` runs on the request context, so a client that gives up mid-delay cancels the wait and the tunnel is never dialed. Hijacked tunnel connections are invisible to `http.Server.Shutdown` and are not drained on exit; as with the WebSocket streams, that belongs to 1.8.
- [x] **2.3 Local certificate authority.** `internal/tlsmitm`: `faultline ca init` creates a CA key and certificate in the user config directory with a ten-year validity and a clear common name like `Faultline Local CA`. `faultline ca path` prints the certificate path. `faultline ca install` prints instructions per OS for trusting it, and on macOS and Linux offers to do it with an explicit confirmation prompt. Never touch the OS trust store without the prompt.
  - Verify (Manual): run `faultline ca init` twice; expect the second run to say it already exists. `openssl x509 -in $(faultline ca path) -noout -subject -dates` shows the expected subject and dates.
  - Note: ran the verify in an isolated `HOME` and the human confirmed it on their machine; the second `init` said `CA already exists in <dir>`, and openssl showed `subject=O=Faultline, CN=Faultline Local CA` with `notAfter` exactly ten calendar years out (`AddDate`, not 3650 days) and `CA:TRUE, pathlen:0`. **Quote the path substitution:** on macOS `os.UserConfigDir` is `~/Library/Application Support`, so the space breaks an unquoted `$(faultline ca path)`; use `"$(faultline ca path)"` and the same goes for the Stage 2 gate. `ca install` prints the path quoted for that reason. The CA key is ECDSA P-256 (cheap to sign the per-host leaves 2.4 will mint), the key file is 0600 and the dir 0700. `ca path` and `ca install` error with a pointer to `ca init` when no CA exists rather than print a path to a missing file. `ca install` builds a per-OS plan (`tlsmitm.TrustPlan`): macOS uses `security add-trusted-cert` on the System Keychain, Linux picks `update-ca-certificates`, `update-ca-trust` or p11-kit `trust` by what is on the PATH, Windows gets `certutil` instructions only; commands run only after a literal `y`/`yes` on stdin, and a closed stdin counts as no. Nothing was ported: neither predecessor had CA code. Also set `SilenceErrors` on the root command since Cobra and `main` were each printing every error.
- [x] **2.4 TLS interception.** When the CA exists and interception is enabled (`--intercept`, default on when CA exists), CONNECT terminates TLS with a per-host leaf certificate minted on demand and cached, then handles the decrypted requests through the full fault pipeline. Events get tier `intercepted` and full method and path. Detect handshake failures that look like pinning or distrust and record an event with a clear `error` such as `client rejected certificate; CA not trusted or pinned`.
  - Verify (Manual): `curl --cacert $(faultline ca path) -x localhost:9001 https://httpbin.org/get`. Expect success and an event with tier `intercepted` and path `/get`. Add a `status 503` rule with path `/get` and repeat; expect 503 with `Faultline-Fault`. Run curl without `--cacert`; expect a TLS error from curl and a clear error event in Faultline.
  - Note: ran the manual verify in an isolated config dir and the human confirmed it on their machine with their real CA; the trusted curl got 200 in 0.73s with an event `GET /get`, `tier: intercepted`; with the 503 rule curl saw `HTTP/1.1 503` and `Faultline-Fault: get-down` without the upstream being called; without `--cacert` curl said `(60) SSL certificate problem: self signed certificate in certificate chain` and the event was `CONNECT`, `status: 0`, `error: "client rejected certificate; CA not trusted or pinned"`, `tier: intercepted`. A host-only 1500ms delay rule applied to an intercepted request too, at 1.66s. **With interception on, the tunnel is not dialed up front and `faults.Dialer` is not consulted:** each decrypted request goes through a second `faults.Transport` with tier `intercepted`, which dials the upstream itself and applies every rule, connection faults included, so a successful interception records one event per request and no `CONNECT` event; a host-only `refuse` rule answers each request with the same synthetic 502 it gives on the plain tier. The CONNECT event only appears when the handshake fails, and that is where the new `error` field on `Event` lives. A certificate alert from the client (`bad certificate`, `unknown certificate authority`) and a plain hang-up mid-handshake are both reported as the rejection, since that is what a distrusting or pinning client does either way; anything else is `TLS handshake failed: <err>`. Leaves are minted by `tlsmitm.Issuer`: one ECDSA P-256 key shared by every leaf (the leaf exists to carry a name, and a key per host would be the slow part of the first request), SAN from SNI with the CONNECT host as the fallback so IP-literal clients that send no SNI still work, 365 days validity re-minted a day before expiry, `NotBefore` an hour in the past for skewed clocks, and the CA cert sent in the chain. ALPN advertises `http/1.1` only; the decrypted side is served by one `http.Server` per tunnel over a single-connection listener, so keep-alive works and `Serve` returns when the client hangs up. `--intercept` is tri-state through `Flags().Changed`: left alone it is on exactly when the CA exists, an explicit `--intercept` with no CA errors pointing at `ca init` instead of downgrading silently, and `--intercept=false` tunnels blindly with a CA present; `serve` prints a `tls:` line saying which. `faults.StripDefaultPort` is exported so the handshake event names the upstream the same way the transports do. Test gotcha: `tls.Conn.Close` over `net.Pipe` blocks on its 5s close-notify deadline at each end, so the handshake test closes the raw pipe ends.
- [x] **2.5 Bypass list.** `--no-proxy` style list of hosts Faultline should tunnel blindly without recording, plus a default bypass for `localhost` and the admin port so Faultline never proxies itself. Also expose which upstreams are bypassed in `/api/upstreams`.
  - Verify (Agent): unit tests for bypass matching including wildcards like `*.internal`. Verify (Manual): add a bypass for `httpbin.org` and confirm no events are recorded for it.
  - Note: ran the manual verify with the real binary: `serve --bypass httpbin.org,*.internal --intercept=false`, then `curl -x` to `https://httpbin.org/get`, `http://httpbin.org/get`, `http://localhost:9000/api/health` and `http://example.com/`; all four answered 200, `/api/events` held only the `example.com` event, and `/api/upstreams` listed `httpbin.org` (2 requests) and `localhost:9000` (1) with `bypassed: true` and no tier next to `example.com` at `tier: plain`. **The default bypass is loopback on every port** (`localhost`, `127.0.0.1`, `::1`), which subsumes the admin port however it is addressed and matches the NO_PROXY 3.1 hands the child; a local service the user wants faulted goes through an explicit route, as SPEC says. **Bypass beats interception**: a bypassed CONNECT is dialed directly, no `faults.Dialer`, no rules, no event, so a client that does not trust the CA still gets through to a bypassed host; a bypassed plain request goes through the same `ReverseProxy` over a bare transport, so hop headers are still stripped but nothing else happens. Bypassed traffic records no events, so `forward.Bypass` counts the hosts it let by (`Saw`/`Seen`) and `/api/upstreams` adds them as rows with `"bypassed": true`, `requests` and `last_seen` but no `tier`, which is now `omitempty`; `bypassed` is always present so a client can switch on it. Entry syntax: `host` (any port, exact, no subdomains, matching how rules match), `*.internal` or the NO_PROXY spelling `.internal` (subdomains, not the apex), `host:port` (that port only), default ports dropped like everywhere else, case-insensitive, IPv6 with or without brackets. `--bypass` is repeatable and comma-separated, its entries add to the defaults and never replace them, a malformed entry is rejected before anything binds, and `serve` prints a `bypass:` line so the operator knows what is being skipped. `forward.NewServer` and `admin.NewServer` each grew a `*forward.Bypass` parameter, nil meaning none, so the existing tests with loopback upstreams keep running unbypassed; admin now imports `proxy/forward` for the `Bypass` type.

**Stage 2 gate (Manual).** With CA initialized, export `HTTPS_PROXY=http://localhost:9001` and `SSL_CERT_FILE=$(faultline ca path)`, then run `curl https://httpbin.org/anything/orders`. Expect success and an intercepted event with the full path. Then delete the env var for the CA and repeat with `-k` removed; expect a clear certificate error event. Finally add a delay rule for the host and a 503 rule for `/anything/*`; verify each triggers as expected.

  - Passed 2026-09-09, run and confirmed by the human. With `SSL_CERT_FILE` set, `GET /anything/orders` came back 200 with an event at `tier: intercepted` and the full path; with it unset curl reported `(60) SSL certificate problem` and the event was `CONNECT`, `status: 0`, `error: "client rejected certificate; CA not trusted or pinned"`. A host-only 2000ms delay rule held the request past two seconds with `faulted: true`; disabling it and adding a `status 503` rule on `/anything/*` gave `HTTP/1.1 503` with `Faultline-Fault` on `/anything/orders` while `/get` stayed 200. The delay had to be disabled before the 503 test because rules apply in creation order and the first match wins. Ctrl-C printed `shutting down` then `shutdown complete`.

---

## Stage 3 — The `faultline run` wrapper

Goal: the five-minute experience. One command wraps any start command and traffic appears with no edits.

- [x] **3.1 Process wrapper.** `faultline run -- <cmd...>` starts the admin server and forward proxy, then runs the child with an environment that sets `HTTP_PROXY`, `HTTPS_PROXY`, `http_proxy`, `https_proxy`, `NO_PROXY` (localhost and the admin port), and forwards stdin, stdout, stderr and signals. Exit code mirrors the child. Print the admin URL on one line at start.
  - Verify (Manual): `faultline run -- curl https://httpbin.org/get`. Expect curl output, a Faultline banner with the admin URL, and an event recorded. `faultline run -- false` exits with code 1.
  - Note: CI was red on ubuntu from this task until 2026-09-09, macOS green throughout. `TestRunInterruptsTheChildWhenTheContextIsCancelled` ran its child as `sh -c "sleep 30"`, which measured the shell rather than `Run`: bash execs a lone command, so the interrupt reached `sleep` and killed it, while dash forks and waits, so the interrupt reached a shell that defers it and left `sleep` untouched until the grace period ran out and killed it. The child is now `sleep` directly, and the bound is half the grace period, since the old bound was the grace period itself and so passed whether the child was interrupted or killed at the end of it. Worth knowing that this exposed a real if currently dormant limit: `relay` signals only its own child, never a process group, so a start command that forks and does not forward signals leaves its own children running. Ctrl-C is unaffected, because the terminal signals the whole foreground group and the child is in it, and `run` builds its context with `Execute` rather than `ExecuteContext`, so nothing cancels it in production. Fixing it properly means `Setpgid` plus handing the terminal to the child's group, which would change this task's settled design; raise it before Stage 3's gate if it matters.
  - Note: verified by the human 2026-09-09. **The banner goes to stderr, not stdout**, so the child owns stdout alone and `faultline run -- curl ... | jq` still works. **NO_PROXY is derived from the proxy's own bypass list** (`localhost,127.0.0.1,::1`, rewritten into NO_PROXY's spelling where a wildcard is a bare leading dot), so what the child skips and what the proxy passes through cannot drift apart; loopback on every port covers the admin port however it is addressed. Both cases of every variable are set, and any the parent already had are removed first so a developer's corporate `HTTP_PROXY` cannot shadow ours. Interrupts are relayed to the child rather than acted on here: the child decides how to stop and Faultline shuts down after it, so `run` does not use `signal.NotifyContext` the way `serve` does. Exit code is the child's, 128 plus the signal when a signal ended it (`exitCode` is split into `exit_unix.go` and `exit_other.go` because `syscall.WaitStatus.Signaled` is Unix-only), carried to `main` as an `exitError` that prints nothing. `serve`'s server bring-up moved into a shared `stack` type (`start`, `banner`, `stop`) with no behavior change. Until 3.2 sets `SSL_CERT_FILE`, curl needs `--cacert "$(faultline ca path)"` for a clean 200; without it the handshake fails but the event is still recorded.
- [x] **3.2 CA trust injection for common runtimes.** Extend the child environment with `SSL_CERT_FILE`, `REQUESTS_CA_BUNDLE`, `CURL_CA_BUNDLE`, `NODE_EXTRA_CA_CERTS`, and `GIT_SSL_CAINFO` pointing at the CA. Create the CA automatically on first run with a one-line notice.
  - Verify (Manual): `faultline run -- python3 -c "import urllib.request;print(urllib.request.urlopen('https://httpbin.org/get').status)"` prints 200 and an intercepted event appears.
  - Note: **the trust variables are only set while Faultline is intercepting.** Without a CA the certificates the child sees are the real ones, so its own `SSL_CERT_FILE` is still the right answer and `runner.Env` leaves it alone; with one, all five are overridden even if the developer's environment already had them, the same way the proxy variables are. `run` no longer shares `serve`'s `resolveInterception`: it uses `ensureCA`, which creates the CA when there is none and prints `ca: created <path>, valid until <date>` on that run only. Creating the CA is not trusting it: `faultline ca install` stays separate and explicit, and a child that reads none of these variables (a browser, a pinned client) is still on its own.
- [x] **3.3 Example applications.** Create `examples/` with four minimal apps used for verification from here on: `go-client` (stdlib client calling httpbin in a loop), `node-fetch` (Node 22, native fetch), `spring-boot` (Java 21, one endpoint that calls httpbin with `RestClient`, plus a second with `WebClient`), and `vite-frontend` (React app calling `/api/*` through the Vite dev server proxy). Each has a README with its start command.
  - Verify (Agent): each example builds and runs standalone and makes at least one upstream call successfully without Faultline.
  - Note: all four verified standalone against `https://httpbin.org`: `go-client` and `node-fetch` logged `200`, `/rest-client` and `/web-client` both returned `{"status":200,...}`, and `/api/get` through the Vite dev proxy returned 200 (the page's button was also clicked in a real browser and rendered the body). **Spring Boot 4.1.1 renamed the starters**: `spring-boot-starter-web` is now `spring-boot-starter-webmvc`, and neither it nor `starter-webflux` provides a `RestClient.Builder` any more, so the app failed to start until the deps became `starter-webmvc` + `starter-restclient` + `starter-webclient` (the last brings Reactor Netty, so `starter-webflux` is gone). Java 21 is the release target, built and run on Temurin 21.0.8; the Maven wrapper is script-only, so no jar is committed. On macOS Netty logs a `MacOSDnsServerAddressStreamProvider` failure at ERROR the first time `WebClient` runs; it falls back to the system resolver and the call succeeds, and the README says so. `WebClient` is still built plain here - 3.6 adds `proxyWithSystemProperties()`. `go-client` lives in the main module, so `make lint` and `make test` cover it; `nilerr` caught its always-nil error return, which is now gone. The Vite dev proxy forwards `/api/*` to `API_TARGET` with the `/api` prefix stripped, defaulting to httpbin so the example stands alone, which leaves 3.7 nothing to do but point that variable at a route port. `examples/README.md` indexes the four.
- [x] **3.4 Go and Python verification.** No new code expected.
  - Verify (Manual): `faultline run -- go run ./examples/go-client`. Expect intercepted events for every call. Add a delay rule and observe the example's logged durations grow.
  - Note: verified by the human 2026-09-09, but only after two findings, and the task did need code. **Go on macOS ignores every CA variable 3.2 sets.** `crypto/x509/root_unix.go` is not built for darwin and `cert_pool.go` says so outright ("On Unix systems other than macOS..."), so a Go child reads roots from the Keychain and nothing else; `faultline ca install` is a hard prerequisite for Go on macOS, where for Python and curl it stays optional. Nothing to fix in Faultline, and the handshake failure already logs the right reason, so surfacing it per host belongs to 3.8. **Second, interception was broken for every reused connection**, which no test caught because each one made a single request: request 1 succeeded, then all the rest failed instantly with 502 and `context canceled`. The reader `Hijack` returns is still plumbed through the serving server's own `connReader`, and the per-tunnel `http.Server` inside interrupts its background read by setting a read deadline, so the `i/o timeout` that abort produces travelled into the outer `connReader`, which read it as the client having gone away and cancelled the connection context the CONNECT request descends from. The inner server ignores that error, since it knows it caused it; the outer one cannot tell. `open` now drains only what the reader has already buffered and returns a plain `net.Conn` with those bytes in front, so nothing reads through the hijacked reader again, and `pipe` and the bypass tunnel lost their reader argument with it. Two tests were added: a reused-connection test through an intercepted tunnel, and one that writes CONNECT plus the request behind it in a single write, since nothing covered the buffered bytes that mechanism exists for. `-race` runs clean now, so 0.2's note about a missing C compiler is out of date.
- [x] **3.5 Node 22 support.** Research whether Node 22 honors `NODE_USE_ENV_PROXY` for native `fetch` (it exists in Node 24; check backport status for the installed 22.x). If yes, set it. If not, implement a small preload script shipped inside the binary and injected via `NODE_OPTIONS=--import`, that routes native fetch through the proxy variables. Document the fallback clearly in the run banner when neither works.
  - Verify (Manual): `faultline run -- node examples/node-fetch/index.js`. Expect intercepted events. Record in this task's note which mechanism was needed for your Node version.
  - Note: verified by the human 2026-09-09. **`NODE_USE_ENV_PROXY` is enough on the installed Node v22.22.3**, so no preload script and no `NODE_OPTIONS=--import` was needed: the backport is present, and proved out of process against a throwaway proxy, where native `fetch` goes direct without the variable and through the proxy with it. It is set unconditionally and Faultline owns it the same way it owns the proxy variables, because a Node too old to know it ignores an unknown variable and the child's runtime is unknown anyway (the start command may be `npm` or `yarn`). It is a proxy concern, not a trust one, so unlike the trust variables it is set whether or not a CA exists. Nothing conditional went into the banner: the mechanism works, so there is no fallback to announce, and a Node-specific line on every run would be noise for the Go, Python and Java children; the help text and the example README carry the explanation instead. Node prints one `[UNDICI-EHPA] EnvHttpProxyAgent is experimental` warning on the first call, left visible because silencing it means `NODE_NO_WARNINGS`, which would hide the child's own warnings too. Enabling this exposed that native `fetch` tunnels `http://` through CONNECT as well, which broke those calls until 3.5a.
- [x] **3.5a Plaintext inside a CONNECT tunnel.** Node's native `fetch` sends `http://` requests through the proxy as `CONNECT host:80` with an unencrypted request behind it, which the interception path meets with a TLS handshake and fails, so 3.5 turned working calls into `fetch failed`. Sniff the first bytes of an intercepted tunnel and route on what is actually there: a TLS record to the interceptor as now, an HTTP/1 request line to the plain-tier pipeline recorded with tier `plain`. Anything else keeps today's behavior, so its failure still explains itself; h2c is not supported. No new tier: plaintext in a tunnel is unencrypted and fully visible, which is what `plain` already means, and calling it `intercepted` would claim the CA is trusted when nothing was intercepted. Only the interception path sniffs. Without a CA the upstream is dialed before the tunnel is opened, so a `refuse` rule can answer the CONNECT itself with 502, and that ordering stays as it is; plaintext already survives that path as an opaque pipe.
  - Verify (Agent): tests covering both branches through a real tunnel, including a reused connection, and `faultline run -- node examples/node-fetch/index.js --url http://httpbin.org/get` recording `plain` events for httpbin.org.
  - Note: verified. Five calls to `http://httpbin.org/get` from native `fetch` came back 200 and recorded as `{"tier":"plain","method":"GET","path":"/get"}`, `/api/upstreams` listed the host at `plain`, and a `status` 503 rule added mid-run flipped the child from 200 to 503 on the next call, so response faults reach this traffic. HTTPS through the same sniff is unchanged: the Node and Go examples still record `intercepted`. **Two narrowings from what was sketched in conversation.** The fallback for unreadable bytes is not a blind pipe but today's behavior, because piping needs an upstream dialed after the tunnel is already open, and a `refuse` rule would then have to be delivered by closing the tunnel instead of answering the CONNECT with 502, which is a second and inconsistent delivery path for a connection fault; an h2c preface is the only realistic case and it now fails with the same clear handshake reason it always did. And the sniff was not extended to the no-CA path: the ordering there is dial-then-open precisely so `refuse` can answer the CONNECT, and reversing it to peek first would break that. Nothing is lost, because plaintext already crosses that path as an opaque pipe and works, just at `encrypted` tier like everything else without a CA. The one-connection `http.Server` and its listener moved out of `intercept.go` into `oneconn.go`, shared by both readable-tunnel paths. `classifyTunnel` requires a real method token followed by a space rather than any uppercase letter, so `PRI ` and `GETTY ` do not read as HTTP/1.

- [x] **3.6 Java and Spring Boot support.** Extend the environment with `JAVA_TOOL_OPTIONS` setting `http.proxyHost`, `http.proxyPort`, `https.proxyHost`, `https.proxyPort`, `http.nonProxyHosts`, and a trust store. Build the trust store on first run by copying the JDK `cacerts` (located via `JAVA_HOME` or `java` on PATH) and adding the Faultline CA, so existing trust is preserved. Document that Reactor Netty based `WebClient` needs `proxyWithSystemProperties()`, and set the example up that way.
  - Verify (Manual): `faultline run -- ./gradlew bootRun` (or `mvnw`) in `examples/spring-boot`, then curl its endpoints. Expect intercepted events for both `RestClient` and `WebClient` calls. Add a `503` rule and confirm the Spring app surfaces the failure.
  - Note: verified 2026-09-09 against the example. Both `/rest-client` and `/web-client` came back 200 with `{"tier":"intercepted","method":"GET","path":"/get"}` events, and a host-only `status` 503 rule turned both into HTTP 500 with `503 SERVICE_UNAVAILABLE from GET https://httpbin.org/get` in the app's own log, so the fault surfaces as a real upstream failure rather than something Faultline-shaped. **The JVM splits `JAVA_TOOL_OPTIONS` on whitespace**, and the macOS config dir sits under `Application Support`, so the first run died with `Unrecognized option: Support/faultline/java-truststore.p12` before Spring started; HotSpot does honour quotes in that variable, so the trust store path is always quoted rather than only when it happens to contain a space. **Faultline appends to `JAVA_TOOL_OPTIONS` instead of owning it** the way it owns the proxy variables, because that is where a developer keeps `-Xmx`, and the JVM lets the last `-D` win so ours still do. The trust store is a copy of the JDK's `cacerts` with the CA imported by the JDK's own `keytool`, which keeps existing trust and means no keystore format is written in Go; it is rebuilt only when the CA or `cacerts` is newer. The JDK is found through `JAVA_HOME`, falling back to asking `java -XshowSettings:properties` where it lives, because walking up from the binary finds nothing on macOS, where `/usr/bin/java` is a stub far from the JDK it runs. No JDK is silent, since most children are not JVMs; a JDK that fails to build a store gets one line and the run continues. `http.nonProxyHosts` is the same bypass list as NO_PROXY, rewritten into Java's spelling (pipes, and the star put back that NO_PROXY drops).
- [x] **3.7 Frontend guidance via explicit route.** Make `faultline run` accept `--route name=url` too, so a Vite project can be wrapped and its dev proxy target pointed at the route port. Add a short `docs/frontends.md` explaining why browser traffic is not caught by the wrapper and how the route solves it.
  - Verify (Manual): `faultline run --route api=https://httpbin.org -- npm run dev` in `examples/vite-frontend`, with Vite's proxy target set to the route port. Load the page, click the button that calls the API, see the event. Add a 2 second delay and see the UI's loading state.
  - Note: verified by the human 2026-09-09 against `examples/vite-frontend`. **`--route-port` stayed on `serve` alone**: route ports are already deterministic from 9100 in declaration order, which is what a checked-in `API_TARGET` needs, and the task asked only for `--route`. `run` parses routes before it touches the CA, so a malformed route fails without creating one. Nothing in the runner changed: the dev server reaches the route over loopback, which the default bypass and the NO_PROXY 3.1 hands the child already send direct, so the page's traffic crosses Faultline exactly once, at the route. `docs/frontends.md` is the first file under `docs/`, and it argues against the alternative as much as for the route: pointing the browser itself at the proxy works but shows every other tab as well and changes the machine rather than the project. A server-rendered framework needs no route, since its calls are the child's already.
- [x] **3.8 Pinning and distrust diagnostics.** When a TLS handshake fails from the client side, the run banner and `/api/upstreams` show a per-host hint: "client did not trust the Faultline CA; see docs/trust.md" including which runtime variables were set. Write `docs/trust.md`.
  - Verify (Manual): `faultline run -- env -u SSL_CERT_FILE curl https://httpbin.org/get`. Expect the hint to appear for `httpbin.org`.
  - Note: verified 2026-09-09, but **not with the command in the Verify block**, which cannot fail on this machine: `env -u SSL_CERT_FILE` leaves `CURL_CA_BUNDLE` set, and Apple's curl reads the Keychain regardless of both, where the CA is installed. A Python client pointed at an unrelated self-signed CA was used instead; the `trust:` line and the `hint` on the host both appeared, and a trusting call later in the same run cleared the hint. **This exposed a real gap: an OpenSSL client over TLS 1.3 was not being recognised as a rejection at all.** The server half of the handshake is finished and its keys have moved on by the time the client gives up, so its alert arrives under the earlier keys and Go reports `local error: tls: bad record MAC`, which fell through to the generic "TLS handshake failed" and so produced no hint. A bad record MAC on a connection whose only traffic so far is a handshake now classifies as a rejection. **The banner cannot carry a per-host hint**, since it is printed before any traffic exists, so the hint reaches the run as one stderr line per host as it happens: the failure repeats with every request the child makes and the advice does not change. The hint clears itself, on the rule that events arrive oldest first and the last handshake outcome for a host wins, so trusting the CA mid-run needs no reset. The variables named in the hint come from `runner.TrustVars`, which is tested against `runner.Env` so the two cannot drift; naming them is the point, because a runtime that ignored all of them (Go on macOS) needs the system trust store, not another variable. `run` now builds the java trust store before the banner rather than after, since the variable list has to exist before the admin server does.

**Stage 3 gate (Manual).** On a fresh machine or clean user account: install the binary, run `faultline run -- go run ./examples/go-client` then the Node and Spring examples in turn. For each, expect intercepted events with no manual trust setup. Time yourself from install to first event; the target is under five minutes.

  - Passed 2026-09-09, run and confirmed by the human on macOS from a cleaned user state: config directory moved aside and the old CA removed from the System Keychain, so the run created its own CA. Install to every example verified took three minutes, 15:01 to 15:04, inside the five-minute target. Go, Node and Spring all recorded `tier: intercepted` with the real method and path; both Spring endpoints, `RestClient` and Reactor Netty `WebClient`, went through. **The "no manual trust setup" part of the target does not hold for Go on macOS, and cannot**: Go reads roots from the Keychain and ignores every CA variable there, so the Go example failed its first handshake, showed 3.8's `trust:` hint, and needed `faultline ca install` before it passed. Node and the JVM needed nothing, because both read what `faultline run` sets them. On Linux `SSL_CERT_FILE` covers Go too and the target holds as written; this is a macOS platform limit rather than something Faultline can close, and it is documented in `docs/trust.md` and 3.4's note. Maven's first-run dependency download is minutes on a clean account and was timed separately, since it is not Faultline setup.

---

## Stage 4 — Faults and behaviors

Goal: the full fault catalogue from SPEC.md with stateful behaviors, and enough test coverage that adding a fault is routine.

- [x] **4.1 Fault interface and registry.** Refactor `internal/faults` so each fault is one file implementing a small interface, registered by name, with its own JSON schema for validation and its tier requirement (`connection` or `response`). The API rejects a response-tier fault on a rule whose only observed traffic is encrypted with a warning, not an error, since interception may come later.
  - Verify (Agent): existing tests still pass; a table test asserts every registered fault validates its own good and bad inputs.
  - Passed 2026-09-09. `rules.Fault` now carries an open parameter bag rather than typed per-fault fields, keeping the flat wire shape through custom JSON, so `internal/rules` names no fault and each fault owns its parameters. The one visible change is the wording of cross-fault validation errors: "code belongs to a status fault, not a delay fault" is now an unknown-parameter message, on the same `fault.code` field. The tier is declared per fault and no longer duplicated as `Fault.IsConnection`; a connection-tier fault is exactly one whose applier implements `Tunneler`, which the table test holds to the declared tier. Design in `docs/specs/2026-09-09-fault-registry.md`.
- [x] **4.2 Connection faults.** `refuse` (already exists, move it), `reset` (close the connection after N bytes or immediately), `hang` (never respond until the client disconnects or an optional cap), `throttle` (bytes per second on the response).
  - Verify (Manual): for each fault add a rule for `httpbin.org` and observe with curl: `refuse` fails to connect, `reset` shows "connection reset by peer", `hang` makes `curl --max-time 3` time out, `throttle` at 1024 bytes/s makes `/bytes/8192` take about eight seconds.
  - Passed 2026-09-09, confirmed by the human running the manual block against `httpbin.org` with curl: `refuse` 502 on plain and "connection refused by rule" on CONNECT, `reset` "Recv failure: Connection reset by peer", `hang` timing out at 3.013s, `throttle` taking 8.29s. `refuse` already lived in the registry, so 4.1 had done the move. The new piece is that a fault owns neither end of the client connection, so `internal/faults/abort.go` holds the one place that produces a RST and every proxy edge routes through it: an aborter in the request context for the request path, a `*ResetError` the tunnel copy recognises for the opaque one. Aborting has to close the socket itself rather than the layer above, or a TLS connection sends a polite close_notify first and the client sees a clean EOF instead of a reset; `bufferedConn`, `peekedConn` and `cutConn` gained `Unwrap` so it can be found. Over an opaque tunnel a reset lands while the client is still writing its handshake, which POSIX reports as a broken pipe rather than a reset - the same event from the other side of the socket. Design in `docs/specs/2026-09-09-connection-faults.md`.
- [x] **4.3 Response faults.** `status` (exists), `rate_limit` (429 with `Retry-After` seconds), `headers` (set and remove on the response), `truncate` (cut the body after N bytes or a percentage, keeping the original `Content-Length` so clients see a broken transfer), `corrupt` (flip bytes in the body), `slow_body` (deliver a correct response over a configured duration).
  - Verify (Manual): each fault on `httpbin.org/json` via curl: `rate_limit` shows 429 and the header; `headers` shows the added header; `truncate` makes curl report a transfer error; `corrupt` produces invalid JSON; `slow_body` makes the response arrive in visible chunks over the duration.
  - Passed 2026-09-09, confirmed by the human running the manual block against `httpbin.org` with curl: `rate_limit` a 429 carrying `Retry-After: 30`, `headers` adding two and removing `Server`, `truncate after_bytes: 100` making curl report "transfer closed with 329 bytes remaining to read" against the original length of 429, `corrupt` at 10 percent leaving 429 bytes that no longer parse, and `slow_body` trickling the whole correct body over five seconds. Two things outside the fault files were needed. **The proxies now copy with `FlushInterval: -1`**: a body smaller than the server's 4KB buffer otherwise lands all at once at the end, so `slow_body` took its duration but showed no chunks, which is half of what the fault is for. **That flushing exposed a data race latent since 4.2**: httputil flushes from a goroutine of its own, while `reset` and `hang` hijack the client socket from inside the body copy, and `-race` reported it in 56 of 60 runs of the reset tests. The aborter now records the request and `WithAbort` resets once the handler has unwound, which is safe because the proxy's flusher is stopped by then and a fault that asks for a reset also fails the read, so nothing reaches the client in between. In the schema, the two shapes these faults need are new kinds, string maps and string lists, and "one of these two parameters" is new as well: `Or` for at least one of a pair, which is `headers`, and `Xor` for exactly one, which is `truncate`. A share of a body and a duration both need a length, so `truncate percent` and `slow_body` measure a body the upstream declared no length for rather than guessing at one. One thing to know for later: `TestResetBreaksAnInterceptedConnection` from 4.2 failed once in roughly 600 runs, wanting `ECONNRESET` and seeing `EOF` where the client's transport retried the request; it is not in the flush path, and it was left alone rather than weakened to accept `EOF`, which would also accept the polite close it exists to rule out.
- [x] **4.4 Behaviors.** Wrap any fault with an optional `behavior`: `percent` (apply to N% of matching requests), `first_n` (fail the first N matching requests then pass), `for_duration` (apply for N seconds after activation then pass), `pattern` (a string like `FFP` meaning fail, fail, pass, repeated). State is per rule and resets when a rule is enabled or edited.
  - Verify (Agent): unit tests for each behavior including reset on enable. Verify (Manual): a `first_n: 2` status rule with three curls gives 503, 503, 200. A `pattern: FP` gives alternating results.
  - Passed 2026-09-09, confirmed by the human running the manual block against `httpbin.org` with curl: `first_n: 2` giving 503, 503, 200, the same rule disabled and enabled again giving 503, 503, 200 a second time, `pattern: FP` alternating, `for_duration: 3` recovering after about four requests, and `percent: 50` faulting 11 of 20. A behavior is a second catalogue beside the faults, in `internal/faults/behavior.go`, because it reuses their parameter schema; the schema gained a scope, so a rejected parameter answers on `behavior.n` rather than `fault.n`, and `Chars` for a string written from a fixed alphabet, which is `pattern`. The state lives in a `Gate`, one decider per rule, and every pipeline over a rule store shares one, or a `first_n: 2` rule would fail two requests per tier rather than two altogether. **Resetting on enable needed something the rule did not have**: disabling and enabling a rule leaves it byte for byte what it was, so a rule now carries a `Revision` the store stamps on every write, off the wire, and behavior state belongs to a revision. One reading was settled by choosing: `for_duration` opens its window at the first request the rule matches, not when the rule was enabled, because a window that starts at enable time can run out while the application is still booting.
- [x] **4.5 Retry detection in events.** Mark events with a `retry_of` reference when the same client makes the same method and path to the same upstream within a short window after a faulted or failed response. Expose `GET /api/sessions/current/report` returning counts: total, faulted, retries, max wait between retries, abandoned (faulted with no retry within the window).
  - Verify (Agent): tests with synthetic event sequences. Verify (Manual): use the Go example, which retries on 5xx, under a `first_n: 2` status rule; the report shows two retries.
  - Agent verify passed 2026-09-09, and the shape the manual block asks for was reproduced end to end against a local upstream through an explicit route: a `first_n: 2` `status 503` rule gave 503, 503, 200, the three events chained `4 <- 5 <- 6`, and the report read `total 3, faulted 2, retries 2, max_retry_wait_ms 1026, abandoned 0`. A single faulted call left alone showed `abandoned` flip from 0 to 1 once its window closed. **The Go example does not retry**: it is a poll loop with a `--every` interval, so what it exercises is that a poll and a retry are the same thing to this heuristic. That is the honest limit of the feature and the reason the window is five seconds, long enough for real client backoff and short enough that a slower loop does not read as one. **There is no per-client partitioning, and the task's "same client" is read as the session's application.** Faultline records no client identity, and the only one available would be the inbound socket address, which is actively wrong here: a client retrying after a connection reset opens a new connection, so keying on it would lose exactly the case that matters most, while the IP alone is `127.0.0.1` for everything and discriminates nothing. Pairing is instead one to one by claim: retry_of is stamped in the recorder while it holds the ring, scanning newest first, so every retry of a candidate is seen before the candidate and two concurrent calls to one path cannot both claim the same attempt. `abandoned` counts an attempt only once its window has closed, or the newest failure would always look given up on, and it covers "faulted or failed" rather than only faulted, matching what the retry link itself is built from. Nothing extra is stored: a retry's wait is recomputed at report time from the pair, so a retry whose original has fallen out of the ring counts as a retry with no known wait.

**Stage 4 gate (Manual).** Using the Spring Boot example, apply in turn: a 30 percent `status 503`, a `first_n: 3` `rate_limit`, a `hang`, and a `truncate`. For each, observe the app's behavior and check the event stream attributes the fault correctly. Confirm the report endpoint's numbers match what you counted by eye.

  - Passed 2026-09-09, confirmed by the human against the Spring Boot example under `faultline run`, each of the four faults applied in turn and removed again. Three things about this gate are worth keeping. **The app never shows the injected status.** The example catches nothing on purpose, so every fault surfaces as Spring's own 500, and what the app proves is that it failed, not how: the real status and the rule id survive only on the event, which is where the attribution was checked. `rate_limit` reads `status 429`, `hang` reads `status 0` with its cap as the duration, and `truncate` reads `status 200` with `bytes_out` at the length the upstream declared rather than the length delivered, which is the whole tell for a cut body. **`hang` needs `max_ms` set here**, because `RestClient` has no read timeout of its own and an uncapped hang would never return. **`retries` on this example measures the operator's pacing, not the application**, since nothing in the Spring example retries and two calls a second apart to one path are a retry to the heuristic whichever made them; the by-eye count has to be read against the calls actually made, and `abandoned` only settles once the five second window closes. There is no event tail yet, so `GET /api/events` stood in for the stream: the WebSocket checks the origin against the host, which no ordinary command line client sends.

---

## Stage 5 — Configuration file, scenarios, and CLI

Goal: everything is declarable in `faultline.yaml`, hot-reloaded, and controllable from the command line.

- [x] **5.1 Config schema and loading.** `faultline.yaml` with `routes`, `bypass`, `rules`, and `scenarios` (a scenario is a name plus a list of rule ids or inline rules). Strict parsing: unknown keys are errors with line numbers. Publish a JSON Schema at `schema/faultline.schema.json` and add the `# yaml-language-server` header hint in examples so editors validate.
  - Verify (Agent): tests for valid files, unknown keys, bad fault parameters, and duplicate ids, each producing a message with file and line.
  - Done. The loader reads the file into a YAML node tree by hand rather than into structs, because line numbers are the whole point and `Node.Decode` drops the strictness a `Decoder` has. Rule shape validation moved from `internal/admin` to `internal/rules` so a rule cannot be valid in a file and invalid over the API; the fault catalogue already owned parameter validation, and the field it names is walked back to the line it is written on. `schema/faultline.schema.json` is generated from the catalogue by `make schema` and a test fails when the checked in copy is stale. Nothing reads the file yet: wiring `serve` to it is 5.2.
- [x] **5.2 Hot reload.** Watch the file; on change, validate and apply atomically. On invalid content, keep the old state and log the error prominently. The API's write operations persist back to the file when one is in use, preserving comments where feasible.
  - Verify (Manual): start with a config, edit a rule's delay in the file, save, curl and see the new delay without restart. Introduce a typo, save, see the error in the log and the old rule still active.
  - Passed 2026-09-09, run end to end against a local upstream through an explicit route declared in the file: a `delay 300` rule gave 0.303s, `ms: 2000` saved into the file gave 2.002s a second later with `config: reloaded` in the log, and a `fault` key that lost its colon gave `ERROR config: the file was not applied, the rules already in memory stay in force path=faultline.yaml line=16 problem="this is not valid YAML: could not find expected ':'"` with the 2s rule still answering. `serve` and `run` gained `--config`; `faultline.yaml` in the working directory is picked up on its own, no file at all is not an error, and a `--config` that names nothing is. Flags win over the file for routes, the bypass list is a union, and the file's rules are in the store before anything listens. **A save is two writes**: truncating the file and writing it again leaves a moment when it is empty, and a poll that lands there would parse an empty file and throw every rule away, so a change is now read only once it has held still for one interval, which costs up to another 500ms before an edit takes effect. The same two writes are also two changes, so a break is reported once per problem rather than once per modification time. Behavior state survives what it should: `first_n: 2` gave 503 either side of an unrelated edit to another rule and then 200, because `Store.Replace` keeps the revision of a rule that is byte for byte what it was and stamps only the ones that differ. Writing back round-trips the file through a `yaml.Node` tree, so comments and quoting survive; blank lines needed help, since the encoder drops them and the way to ask for one back is a leading newline on a head comment, an otherwise empty head comment being a blank line on its own, and a test holds `examples/faultline.yaml` to coming back byte for byte. **A rule declared inside a scenario is edited inside that scenario**, and deleting a rule also drops the scenario entries naming it, or the file Faultline wrote would not load; 5.3 should keep that in mind when it starts moving rules between scenarios. Faultline's own write updates the stamp under the same lock, so it never comes back as a reload. An API change first applies a hand edit the watch has not read yet and then lands on top of it, and while the file does not parse an API write is refused with 409 naming the line rather than overwriting a file Faultline cannot read. Routes and the bypass list are read once: a change to either warns that it needs a restart and the rules in that same save still apply.
- [x] **5.3 Scenarios.** `POST /api/scenarios/{name}/activate|deactivate`, `GET /api/scenarios`. Activating enables the scenario's rules and resets their behavior state; deactivating disables them. Only one scenario active at a time in this release; activating another deactivates the current one.
  - Verify (Manual): define two scenarios, activate one, check `GET /api/rules` shows the right enabled flags, activate the other, check they flipped.
  - Passed 2026-09-09 against the built binary, a `python3 -m http.server` upstream and an explicit route: `api-down` on gave `{"id":"fails","enabled":true}` with `gateway` off and 503 503 200 through the route from its `first_n: 2`, three more requests gave 200 200 200, activating `api-down` again gave 503 503 200 a second time, `gateway-down` on flipped the flags to `fails` off and `gateway` on and the route to 502, deactivate put both off and the upstream's 200 back, and `ghost` gave `404 {"error":"no scenario named \"ghost\""}`. `Scenario` moved out of `internal/config` into `internal/rules` beside the rule store, so one type serves the file and the API, and a `Scenarios` store next to it holds the file's list plus the single active name. Scenarios are not created or edited over the API; only activate and deactivate are. **The behavior reset is a re-stamp, not a fake edit**: 4.4 keys behavior state on a rule's id and revision, so the new `Store.Switch(on, off)` turns both sets over under one lock and stamps every rule in `on` even when it was already on, which is exactly what "start this rehearsal over" means and is why re-activating the scenario that is already active is the way to ask for that alone. `Switch` is also the atomicity: one write, so no request ever sees a rule of the outgoing scenario next to one of the incoming, and a rule both name is turned on rather than off. **Nothing is reference counted**: a rule's enabled flag is whatever the last write said, so a rule two scenarios name follows the newer of them, a rule enabled by hand is turned off by a scenario that names it, and rules no scenario names are never touched. Deactivating a scenario that is not the active one is not an error and not a no-op either: its rules go off, which is what was asked for, and whatever is active stays active. **An activation is persisted like any other rule change**, through the same `File.Change`, because a later save of the file is applied on top of memory and flags that lived only in memory would be silently put back the first time anyone touched the file; the run showed `enabled: true` written into the file for `gateway` and no reload logged, since Faultline's own write updates the stamp. Which scenario is active is deliberately *not* written: the schema has no place for it, the file is committed, and a checkout should not arrive with a rehearsal already running, so a restart forgets the name and keeps the flags. A reload re-seeds the list, so a scenario added or renamed in the file can be activated without a restart; the active one survives a reload if the file still declares it by name and is dropped with a warning naming it if not. One thing 5.2 left behind bit here: Faultline's own write never comes back as a reload, so deleting a rule through the API, which already drops the scenario entries naming it from the file, had to drop them from memory too, hence `Scenarios.Forget` on the delete path. 5.4 wraps these three endpoints and nothing more; 5.5 should activate before the child starts and deactivate after it exits, and gets a clean report for free because activating resets the counters; 6.5 needs only the `active` flag on the list, since `Switch` notifies and `rules_changed` already goes out.
- [x] **5.4 CLI commands.** `faultline rule list|add|rm|enable|disable`, `faultline scenario list|on|off`, `faultline events tail|export`, `faultline upstreams`. They talk to a running instance over the API, printing tables for humans and `--json` for machines.
  - Verify (Manual): drive the whole Stage 1 gate again using only CLI commands.
  - Passed 2026-09-09, the whole Stage 1 gate driven from the command line against the built binary, two routes to `httpbin.org` and `postman-echo.com`: `rule add --name "Httpbin is slow" --host httpbin.org --fault delay --set ms=2000` and the same for a `status code=503` on the echo route, `rule list` showing both, 200 in 2.585s with a real 287 byte body through 9100, 503 in 0.001s carrying `Faultline-Fault: echo-is-down` through 9101, both events attributed by `events tail` live and by `events export` after, and `shutting down` then `shutdown complete` on Ctrl-C. The layout's `clients/go` was built first and the CLI is a printer over it, so the wire shape is written down once; it aliases the server's own types rather than restating them, which is why it imports `internal/admin` and why `admin.scenarioResponse` became `admin.Scenario`. **`rule add` needed a way to write a rule that does not grow a flag per fault parameter**, since the catalogue is open: the match has one flag per field and both catalogues are reached as a type plus repeated `--set name=value` (`--behavior-set` for the behavior's own bag), a value being read as JSON when it is JSON and as text when it is not, so `ms=2000` is a number, `set={"X-Test":"1"}` is a map and `'body="200"'` is the way to write text that looks like a number. Nothing on the command line judges a parameter; the API validates and its answer names the field, and the CLI does not even require `--fault`, because the server's own message lists the catalogue. `--from <file|->` reads a whole rule for scripts and replaces the shaping flags rather than mixing with them. A rule with no `--name` names itself `delay on api.stripe.com`, because the API requires a name and the gate is driven by hand. **The Origin check needed nothing weakened**: `coder/websocket` allows an absent Origin and refuses a wrong one, so the client sends the address it is dialling and the Stage 4 note's "no ordinary command line client sends one" is answered by sending one. `events export` writes JSON Lines rather than an array, because the file exists for another tool - it streams, appends, and `wc -l`, `grep` and `jq -c` all read it a line at a time - and `--json` still means the API's own document everywhere, which on the stream is the envelope and not the bare event. `rule rm` is the one command `--json` cannot answer, a delete being 204 with no body; it prints its one line either way. Docs in `docs/cli.md`, linked from the README.
- [x] **5.5 Scenario runs with report.** `faultline run --scenario <name> -- <cmd>` activates the scenario, runs the command, deactivates, and prints the session report. `--report json` writes it to a file. Non-zero exit if the child fails.
  - Verify (Manual): `faultline run --scenario orders-flaky -- go test ./examples/go-client/...`. Expect the test output followed by a report table with faulted counts and retries.
  - Passed 2026-09-09 against the built binary, `httpbin.org` through the forward proxy with interception, and a scenario `orders-flaky` whose `first_n: 2` `status 503` rule is written inside it: the run printed `scenario: orders-flaky active for this run` under the banner, then the child's `503, 503, 200`, then `REQUESTS 3, FAULTED 2, RETRIES 2, MAX RETRY WAIT 1002ms, ABANDONED 0`, then `report: wrote report.json` holding the same five numbers, and left `enabled: false` written back into the scenario's rule. A child exiting 3 gave exit 3 with the report still printed, `--scenario ghost` was refused before the child started, and a SIGINT to Faultline reached the child, ended it, and still turned the scenario off and wrote the report. **The roadmap's `go test ./examples/go-client/...` does not exist**: the Go example has no tests, it is the poll loop 4.5 described, so `go run ./examples/go-client --count 3 --every 1s` stood in for it; that is the same path end to end, and the retries the report counts are a poll loop's, which 4.5's note already says is the same thing to the heuristic. Adding a real retrying test to the example was left alone rather than widened into here. **The report is printed for every run, not only a scenario run.** A session is a session; a run that faulted nothing still says how many calls went out, and a `REQUESTS 0` table is the first sign that wrap mode is not carrying the child's traffic at all. **`--report` takes the file, not a format.** The roadmap writes `--report json`, but the report has exactly one machine-readable shape, so a format flag would have a single legal value and would still need somewhere to put it; one flag naming the file is what a CI job actually wants. `--report json` therefore answers "--report takes the file to write, not a format", as do the other format words someone might reach for, rather than quietly writing a file called `json`; the destination's directory is checked at flag time too, so a bad path fails before the test suite runs rather than after. **`run` drives its own admin API rather than the stores it just built**, through 5.4's Go client, which grew the `Report` method it was missing. Activation has to go through `admin.Server.change` to be persisted the way 5.3 requires, and reading the recorder in memory would be a second way to do something that already has one; the price is one HTTP hop to loopback, which the bypass list keeps out of the proxy anyway. **`abandoned` will almost always read 0.** The report is taken the moment the child is gone, and 4.5 only counts a failure as abandoned once its five second window has closed, so the failures at the end of a run - exactly the ones a suite ends on - are still undecided. Waiting the window out would add five seconds to the end of every `faultline run`, so the number stays as it is and the docs say what it means. The child's exit code is never lost: a scenario that will not turn off or a report file that cannot be written is printed on stderr and only becomes the command's own error when the child itself succeeded. One thing seen in passing and not new: writing the file back sorts a fault's parameters, so `code` before `body` came back as `body` before `code`, which is 5.2's round trip.
- [x] **5.6 Init command.** `faultline init` writes a commented starter `faultline.yaml` with one example scenario.
  - Verify (Manual): run it in an empty directory, then `faultline serve`; expect no validation errors.
  - Passed 2026-09-09 in an empty directory against the built binary: `faultline init` wrote `faultline.yaml`, `faultline serve` came up on it with `config: faultline.yaml, watched for changes` and no validation error, and `rule list` showed `slow-stripe` and `stripe-503` both off. **The starter is its own file, not `examples/faultline.yaml`.** It is `internal/config/starter.yaml`, embedded as `config.Starter`, because the two answer different questions: the example is a reference that shows every section at once, a live route and a bypass entry included, while the starter is written into a stranger's empty directory and therefore has to be inert. A route in it would bind 9100 on the next `serve`, and fail the command outright when something else already has that port; a `bypass` entry would hide hosts from the event stream before anyone asked. So the starter carries 5.1's schema header, one disabled `delay` rule, one scenario whose inline `status` rule waits for its scenario, and `bypass` and `routes` as commented syntax at the end, with the comments saying that the hosts are an illustration to be edited. Tests hold all three properties - it parses, nothing is enabled, nothing is bound - since a starter that faults traffic on sight is a trap, and the file sits next to the loader rather than next to the command because this package is what decides what a configuration file may say. **The refusal to overwrite is the file system's**: `O_EXCL`, so nothing can appear between a stat and a write, and the message names both ways on, `faultline init <file>` and `--force`; a path that is a directory is told apart from a file that is already there, because "already there" would be a lie. The mode is 0644, the mode `internal/config` gives a file it writes itself, so a later API write does not quietly change it.
  - **Walking the Stage 5 gate turned up one thing the gate as written cannot do.** A scenario that delays *and* fails the same host does not exist: `Transport.RoundTrip` takes the first matching rule and stops, behavior included. With the delay rule first, all 52 calls were 200 after 1.6s and the `first_n: 2` 503 never fired; with the 503 rule first, calls 1 and 2 were 503 in 10ms and 1ms and calls 3 to 5 came back in 136ms with no delay at all, the spent rule still shadowing the one behind it. Everything else in the gate held: the hand-added scenario loaded, `run --scenario httpbin-bad` printed `scenario: httpbin-bad active for this run` and ended with `REQUESTS 52, FAULTED 52, RETRIES 51, MAX RETRY WAIT 2003ms, ABANDONED 0`, editing `ms: 1500` to `4000` while it ran logged `config: reloaded path=faultline.yaml rules=4` and the next call took 4.135s instead of 1.636s, and deactivation wrote `enabled: false` back with the hand-written comment and the new delay intact. One thing seen in passing and not this task's: `faultline run -- go run ...` cannot be interrupted through Faultline when there is no terminal, because `run` hands the signal to its child and `go run` does not pass it on to the binary it built; a SIGINT to the application itself ended the run and printed the report as it should.

- [x] **5.7 Behavior fall-through.** A rule whose behavior declines a request hands it to the next matching rule instead of sending it upstream untouched. The first enabled rule that matches *and whose behavior accepts* decides; a rule with no behavior is unchanged, so an unconditional rule still shadows the ones behind it and store order is still precedence. This closes the open question the stage gate raised: a scenario that fails the first calls and slows the rest is a `first_n` `status` written above a `delay`. Spec in `docs/specs/2026-09-10-behavior-fall-through.md`.
  - Verify (Agent): a `first_n: 1` 503 ahead of a 500 on one host gives 503 then 500; a rule behind the one that applied does not spend its behavior; the same on the connection tier, a `first_n: 1` `refuse` ahead of a `delay` giving a refused CONNECT then a delayed dial.
  - Verify (Manual): the Stage 5 gate as written, a scenario whose `first_n: 2` 503 rule is written above a delay rule for `httpbin.org`, run under `faultline run --scenario`, giving 503, 503, then delayed 200s.
  - Passed 2026-09-10, agent verify under `-race` and the manual gate confirmed by the human against the built binary: a scenario with a `first_n: 2` 503 written above a 1500ms delay for `httpbin.org`, run under `faultline run --scenario`, gave two 503s and then delayed 200s, and editing the delay while it ran took effect on the next call. One thing seen on the way: `faultline run -- go run ./examples/go-client` from an empty directory fails before any request with `go.mod file not found`, because the child inherits the working directory; the guide now builds the example first or uses `go run -C`. The change is the gate check moving inside the match loop of `Transport` and `Dialer`, so `match` now returns the rule together with its built fault and a rule is skipped for any of three reasons: it does not match, its fault will not build, or its behavior said no. Behavior state advances for every rule consulted and for no other, which is what makes a shadowed `first_n` still whole when the rule ahead of it runs out, and a test holds that. Fall-through is for a behavior declining only; a response fault that cannot act on a bare connection was already skipped by the dialer, and one that cannot act on encrypted traffic is still that rule's event with the tier saying why. Composing faults across rules, delayed 503s, was considered and left closed: it needs an order of application, a story for two response faults on one request, and a `Faultline-Fault` header naming several rules, and if it ever comes it belongs inside one rule as a list of faults. One thing to carry into Stage 6: order is now how a scenario is built, the file gives full control over it, and rules created over the API land in creation order with no way to reorder, so 6.4 should at least show the order.

**Stage 5 gate (Manual).** From an empty directory: `faultline init`, edit the file to add a scenario that delays and fails httpbin, `faultline run --scenario <it> -- go run ./examples/go-client`, read the report. Then, while it runs, edit the file to change the delay and observe the change live.

  - Passed 2026-09-10, confirmed by the human against the built binary once 5.7 landed: a scenario with a `first_n: 2` 503 above a 1500ms delay for `httpbin.org` gave two 503s and then delayed 200s, editing the delay while it ran took effect on the next call, and the report counted every request as faulted.

### How to run the Stage 5 verification

Every task in this stage was verified by an agent as it was built, and the notes
above record what each one saw. What follows is the whole stage in one pass, for
the human gate. It takes about fifteen minutes. `httpbin.org` has to be
reachable, and two terminals are easier than one.

Build once, and keep `faultline` on the path for the rest of it:

```
make build
export PATH="$PWD/bin:$PATH"
```

Three things below will look wrong if you are not expecting them, so they are
worth knowing before you start rather than after:

- **A match on a host needs the port when the upstream is not on 443 or 80.** An
  event from a local upstream records `127.0.0.1:8099`, so `host: 127.0.0.1`
  matches nothing.
- **Order is precedence, and a behavior that declines hands the request on.**
  The first rule that matches and whose behavior accepts decides. A rule with no
  behavior always accepts, so a `delay` written above a `status` on the same host
  means the `status` never fires; a `first_n` `status` written above a `delay`
  gives the failures and then the slow responses. See the note on the gate
  below.
- **A saved edit takes effect in about a second, not instantly.** A change has to
  hold still for one poll before it is applied, because a file being saved is
  briefly empty and applying that would drop every rule.

#### 1. Hot reload (5.2)

In a scratch directory, with a local upstream so the timings are unambiguous:

```
python3 -m http.server 8099    # leave running in another terminal
```

```yaml
# faultline.yaml
routes:
  - name: local
    upstream: http://127.0.0.1:8099
    port: 9100

rules:
  - id: slow-local
    name: Local is slow
    match:
      host: 127.0.0.1:8099
    fault:
      type: delay
      ms: 300
```

```
faultline serve
```

Expect a banner including `config: faultline.yaml, watched for changes` and
`route local: http://127.0.0.1:9100 -> http://127.0.0.1:8099`.

| Do this | Expect |
| --- | --- |
| `curl -s -o /dev/null -w "%{time_total}\n" http://127.0.0.1:9100/` | about `0.30` |
| Change `ms: 300` to `ms: 1500`, save, wait a second, curl again | about `1.50`, and `INFO config: reloaded path=faultline.yaml rules=1` in the log. No restart. |
| Break the file: delete the colon after `fault`, save | `ERROR config: the file was not applied, the rules already in memory stay in force` naming the line and the problem |
| Curl again while it is broken | still about `1.50`: the last good rules are still in force |
| `faultline rule add --name Probe --host x --fault status --set code=503` | refused with 409, naming the line, because a change would have to be written to a file Faultline cannot read |
| Fix the file, save | `INFO config: reloaded`, no restart needed |
| `faultline rule add --name "Probe" --host 127.0.0.1:8099 --fault status --set code=503` | the rule is appended to `faultline.yaml` with every comment and blank line intact, and no reload is logged for Faultline's own write |
| `faultline rule disable probe` | `enabled: false` written in place in the file |

#### 2. Scenarios (5.3)

Add two scenarios to the same file, each naming one rule, and restart `serve`.

| Do this | Expect |
| --- | --- |
| `faultline scenario list` | both listed, `ACTIVE` no |
| `faultline scenario on api-down` | that one `ACTIVE` yes |
| `faultline rule list` | only that scenario's rules enabled |
| Drive traffic | the scenario's fault applies |
| `faultline scenario on the-other-one` | the flags flip in one step: the first scenario's rules off, the second's on |
| `faultline scenario off the-other-one` | everything off, traffic back to normal |
| `faultline scenario on ghost` | `no scenario named "ghost"`, exit 1 |

To see the behavior reset, put a `first_n: 2` `status 503` rule in a scenario,
activate it, and curl three times: `503 503 200`. Curl twice more: `200 200`.
Activate the same scenario again and curl three times: `503 503 200` once more.
Activating is how you ask for the counters to start over.

#### 3. CLI (5.4)

This is the Stage 1 gate driven from the command line. With `serve` running and
two routes, or through the forward proxy:

| Do this | Expect |
| --- | --- |
| `faultline rule add --name "Httpbin is slow" --host httpbin.org --fault delay --set ms=2000` | a one-row table, the rule created |
| `faultline rule list` | every rule, with match, fault and behavior in columns |
| A call to httpbin through Faultline | about two seconds slower |
| `faultline rule add --host httpbin.org --fault status --set code=503 --name "Down"` | remember that order is precedence: the delay has no behavior, so disable it first or this never fires |
| `faultline events tail` | live rows as traffic arrives; Ctrl-C exits cleanly, code 0 |
| `faultline events export` | one JSON object per line, `retry_of` visible on a retry |
| `faultline upstreams` | each host with tier, counts and last seen |
| `--json` on any of them | the API's own JSON |
| `faultline rule enable ghost` | `no rule with id "ghost"`, exit 1 |
| `faultline rule add --name x --host y --fault delay --set ms=abc` | `ms must be a whole number (fault.ms)`, exit 1 |
| `faultline rule list --admin http://localhost:9999` | `no Faultline is listening at http://localhost:9999; start one with 'faultline serve'`, exit 1 |

#### 4. Scenario runs and the report (5.5)

With a config declaring a scenario whose rule is a `first_n: 2` `status 503` on
`httpbin.org`:

```
faultline run --scenario orders-flaky --report report.json \
  -- go run ./examples/go-client --url https://httpbin.org/get --count 3 --every 1s
```

Expect `scenario: orders-flaky active for this run` under the banner, the
child's own output showing `503`, `503`, `200`, and then:

```
report: this run
REQUESTS  FAULTED  RETRIES  MAX RETRY WAIT  ABANDONED
3         2        2        1000ms          0
```

`report.json` holds the same five numbers. Also worth trying:

| Do this | Expect |
| --- | --- |
| A child that exits non-zero | that exit code from Faultline, with the report still printed |
| `--scenario ghost` | refused before the child starts, and the names the file does declare |
| `--report json` | refused: `--report` takes the file to write, not a format |
| A run with no `--scenario` | still a report, because a session is a session |

Two things to know. `RETRIES` on the Go example counts the poll loop, not
application retries, which is what the 4.5 note says the heuristic can and
cannot tell apart. And `ABANDONED` reads 0 almost always, because the report is
taken the moment the child is gone and an abandonment only counts once its five
second window has closed.

#### 5. Init (5.6)

```
mkdir /tmp/fresh && cd /tmp/fresh
faultline init
faultline serve
```

Expect `wrote faultline.yaml` and a short note about editing the hosts, then a
clean start with no validation error. `faultline rule list` shows two rules,
both off, and `faultline scenario list` one scenario, not active: nothing in a
freshly written file touches your traffic. `faultline init` a second time
refuses to overwrite and names both ways on, `faultline init <file>` and
`--force`.

#### 6. The stage gate

From an empty directory: `faultline init`, edit the file to add a scenario
against `httpbin.org`, run the Go example under it, read the report, and while
it runs change the delay in the file and watch the next call take the new time.
The empty directory has no `go.mod`, so `go run ./examples/go-client` fails
there before making a request; run `go run -C <faultline checkout>
./examples/go-client`, or build the example once and run the binary.

**The gate's wording asks for a scenario that delays *and* fails httpbin, and
5.7 is what makes that possible.** Write the `first_n: 2` `status` rule above the
`delay` rule. The first two calls are 503s, and every call after them takes the
delay, because a rule whose behavior has run out hands the request to the next
matching rule. The other order does not work and is not meant to: a `delay` with
no behavior accepts every request, so a `status` written below it never fires.
Before 5.7 neither order worked, since the first matching rule decided whether
or not its behavior applied, and a spent `first_n` shadowed everything behind
it; the stage was gated with a scenario that delayed *or* failed, and this
paragraph carried the open question 5.7 then answered.

---

## Stage 6 — Web UI

Goal: everything above is visible and controllable in the browser, embedded in the binary.

- [x] **6.1 Port the existing UI.** Move the previous `resilience-proxy/web` Next.js app into `web/`, upgrade dependencies, keep static export, embed the output into the binary served at `/`. Replace the old WebSocket rule-push protocol with REST calls for mutations and the read-only event stream for updates.
  - Verify (Manual): `make build` embeds the UI; open `localhost:9000`; the page loads with an empty request list and no console errors.
  - The old app's data layer matched nothing here, so the shell was ported and the types, API access and components were written against the real API. The Metrics view became 6.7 rather than arriving early; the Settings view was dropped, since its target URL and proxy port are not editable concepts. `go:embed` cannot reach a parent directory, so the embed lives in `web/` beside the export, behind a `ui` build tag: a checkout with no Node still compiles, and `/` then explains what is missing. Spec in `docs/specs/2026-09-10-web-ui-port.md`.
- [x] **6.2 Live request stream and inspector.** Show events live with method, host, path, status, duration, tier badge, and a fault badge linking to the rule. Filters for host, method, status class, and faulted only. Clicking an event opens headers and body for request and response when intercepted, or a clear "encrypted" notice otherwise.
  - Verify (Manual): run the Go example wrapped; watch events stream; filter to faulted only; open one and read its body.
  - Passed 2026-09-10, confirmed by the human. Two things came back from the first pass and were fixed before it was signed off: the fault badge shared the tier column and read as loose text next to an unrelated pill, so `Fault` is now its own column and a faulted row carries an amber left bar and a tint, readable before any of its text is; and the status filter appeared to do nothing, which turned out to be the id collision below rather than the filter. The large-body case in the hand-written test script was wrong as first written - a 70 KB query string earns a 414 from httpbin and truncates nothing, so it sends the bulk as a body instead, which exercises both sides at once.
  - **The inspector had no backend.** Nothing captured headers or bodies; `events.Event` is metadata only, and the roadmap mentions capture just once, at 9.4's `--no-bodies`, which assumes it already exists. So this task built it: `internal/capture` holds a `Capture` per event id under a 32 MB byte budget, 64 KB per body, dropping oldest-first, and `GET /api/events/{id}/capture` reads one. Captures live apart from events on purpose - putting bodies on `Event` would put every body on every stream frame and in the thousand-event ring. Only `faults.Transport` captures, so encrypted traffic has no capture by construction rather than by a special case, and that absence is what the UI's notice reads off. **Bodies are captured after the fault pipeline**, so a `truncate` shows the 20 bytes the client got rather than httpbin's 429; capture is two-phase because an event is recorded at the response headers while its body is still streaming. **The fault badge names the rule but is inert**: "linking to the rule" waits for 6.4's editor to have somewhere to link to. Filters run client-side, since a refetch would drop everything that arrived on the socket since. Spec in `docs/specs/2026-09-10-live-request-stream-and-inspector.md`.
  - **Verification turned up a bug older than this task.** Event ids are a per-process counter that restarts at 1, but the browser's list was seeded once and then only ever appended to, so a list spanning a restart of the binary held two different events sharing an id. They collided on their React key and left rows on screen that the filtered data no longer contained, which read as "the status filter does nothing". The stream now re-reads `GET /api/events` on every connect rather than only the first, so the list always belongs to the process on the other end of the socket; events that arrive while that read is in flight are kept, and a read that fails leaves the list standing. That also closes a gap nobody had noticed: events recorded while the socket was down used to be lost for good, and now arrive with the re-read.
- [x] **6.3 Upstreams panel.** List observed upstreams with tier, counts, error rate, and a per-host hint when the CA is not trusted. Quick actions: add delay, add 503, bypass.
  - Verify (Manual): after running the examples, see each upstream listed with the correct tier; click "add delay" and confirm the next requests slow down.
  - Passed 2026-09-10, confirmed by the human against the built binary: every upstream listed with its tier and counts, the delay toggle taking the next requests to 2.004s and back to 0.6ms when clicked again, the 503 toggle answering 503 and round-tripping through `faultline.yaml`, a hand edit to `bypass:` in force in about a second, and `DELETE /api/bypass/localhost` refused with 409. Two things came back from that pass and were fixed before it was signed off: the quick actions and the rest of the shell's controls had no pointer cursor, and the bypass action did not stop an open tunnel, which is the fourth note below. A third came from the human's read of the panel: a hint under a row sat between two dividing lines, so it was not clear which row it spoke for. A strip now names its host and shares one block with the row above it, and the line between rows falls under the strip rather than above it.
  - **The bypass quick action had no backend.** `forward.Bypass` was built once at startup, `config.Save` wrote only rules, and a hand edit to `bypass:` logged "restart faultline to apply it". So bypass became what rules already are: live, persisted and hot-reloadable. `Add`, `Remove` and `Replace` sit under the list's existing mutex, `POST /api/bypass` and `DELETE /api/bypass/{host}` go through the same persister as a rule write, and a reload applies the list instead of asking for a restart. The `DefaultBypass` entries cannot be removed at all - loopback is how Faultline reaches itself - and they are never written into somebody's file. Error rate is a server-side `errors` count (a 5xx, or a request that never got a status; a 4xx is the upstream answering), so the CLI gained the column too rather than the rate living only in the browser. The two degrade actions need no new API: the panel finds the rule it made by shape - enabled, host-only match, that fault type - because a rule id may not hold a colon and a host may carry a port, and matching by shape also lights the button for the same rule written by hand. Spec in `docs/specs/2026-09-10-upstreams-panel.md`.
  - **Two things only the running binary showed, both about what "bypassed" means.** A row folded "is on the bypass list" and "was passed through untouched" into one flag, but the list belongs to the forward proxy and an explicit route never consults it, so a routed local upstream matched the default `127.0.0.1` entry and read as bypassed while its traffic was being proxied, recorded and faulted. And the flag was computed with pattern matching while removal was exact text, so the toggle sat lit on a host that no un-bypass click could remove and the row printed "is on the bypass list" above "is not on the bypass list". A row now carries `bypassed` for what was skipped and `bypass_entry` for the entry that covers the host - often not the host, since a portless entry covers every port and `*.internal` every subdomain. The toggle is lit whenever an entry covers the host and disabled when that entry is broader than the row, naming the entry to edit instead; `Remove` stays exact, so un-bypassing one subdomain cannot delete a whole wildcard. A third case came out of tracing the manual steps rather than running them: a host bypassed part way through has events from before and passed-through requests since, and the old code emitted a row for each because a bypassed host used to be one that could never have events. Those fold into one row now, whose tier and faulted and error counts are what Faultline actually saw and whose `requests` is every call the host received either way.
  - **A fourth came out of the human's pass, and it was the bypass action's real bug.** The list was consulted once per CONNECT, and a client keeps its tunnel open, so bypassing a host with a request every second changed nothing at all: `serveOneConn` went on serving the same intercepted tunnel, recording and faulting every request on it, until the client happened to reconnect, which it never did. The list is now consulted per request inside an open tunnel, intercepted and plaintext alike, and a bypassed request travels over the pipeline's own base transport, so it keeps the same connections and the same trust with none of the rules or the recording. Interception itself cannot be taken back for a connection already terminated, and breaking the connection to force a fresh CONNECT would be worse: a bypass is how somebody takes a host out of the way, and it should not be the thing that finally breaks it. `faults.Transport` grew a `Base()` accessor for this, which is also what keeps the bypassed path from inventing a second trust posture.
- [x] **6.4 Rule editor.** Create and edit rules with fields driven by each fault's schema, so a new fault appears in the UI without UI changes. Enable and disable toggles. Behavior section with the four behavior types.
  - Verify (Manual): create a `first_n: 2` status rule from the UI; curl three times; see 503, 503, 200 in the stream.
  - Passed 2026-09-10 against the built binary, driven through the browser: the rule created from the form, written to `faultline.yaml`, and 503, 503, 200 from three curls. Editing it to 504 and saving gave 504, 504, 200, so a behavior's state starts over on an edit, as `Revision` intends.
  - **The catalogue had no way out of the binary.** `faults.Schema` describes a parameter completely - kind, required, bounds, alphabet, and Or/Xor pairing - but nothing published it over HTTP, so "fields driven by each fault's schema" had no source to be driven from. New `GET /api/catalogue` projects `faults.All()` and `AllBehaviors()`, the same descriptor `internal/config/jsonschema.go` already reads. The form maps a parameter's *kind* to a control and never names a fault, so the eleven faults and four behaviors all render from one of four widgets; only a new `faults.Kind` would need UI work. `Field` also gained `Desc`, filled in for all 18 parameters, because a form labelled `retry_after_sec` with nothing under it is the config file with worse ergonomics. The published JSON Schema carries those descriptions too, so hovering a parameter in `faultline.yaml` now says what it does. Spec in `docs/specs/2026-09-10-rule-editor.md`.
  - **Validation stayed on the server, deliberately.** The form applies the schema's bounds as input attributes and otherwise submits and maps the 400's `field` onto the control that holds it - `fault.ms`, `behavior.pattern`, `match.path` all land under the right input. A second validator in TypeScript would be a second thing to keep true.
  - **The fault badge is no longer inert**, closing the note 6.2 left: in the inspector it opens its rule in the editor. In the stream's rows it stays a label, since a badge inside a clickable row would have two meanings for one click.
- [x] **6.5 Scenarios panel and report.** List scenarios, activate and deactivate, show the current session report live, and a "reset session" button.
  - Verify (Manual): activate a scenario, generate traffic, watch the counts update, reset, watch them clear.
  - Passed 2026-09-10, confirmed by the human against the built binary: a scenario saved from the panel, turned on there, giving 503, 503, 200 through an explicit route, the tiles climbing to 5 requests and 2 faulted as the traffic went out, the counts going to zero on Reset session, and turning the scenario off putting the 200s back. Spec in `docs/specs/2026-09-10-scenarios-panel-and-report.md`.
  - **The panel needed one thing the Stage 6 gate asks for and no endpoint could do.** The gate walks SPEC's five minutes in the browser, "save what you did as a scenario" included, but 5.3 shipped activate and deactivate on purpose and scenarios are authored in the file, so creation was folded in here rather than the gate being amended: `rules.Scenarios.Add`, `POST /api/scenarios` over the same persister path an activation uses, and `config.Save` grew the scenario list with `Applier.Scenarios()` beside `Rules()` and `Bypass()`. A created scenario **names rules that already exist** - a scenario is a name for a set of rules, not another way to declare them - and it **arrives inactive however its rules stand**, because activating starts the behavior state of every rule it names over and saving should not do that as a side effect.
  - **The file write is append-only, and that is not a shortcut.** A scenario the file already declares keeps the rules list it was written with, because that list may hold a whole rule written in place and writing it back as a bare id would leave a file that no longer loads; dropping the entries that named a deleted rule was already `dropGone`'s job. Appending at the end is also what keeps a new scenario loadable, since `Load` reads every rule before any scenario and inline rules join the set in declaration order. Passing no scenarios therefore cannot destroy the ones in the file, which is what the config watcher's own tests rely on. One thing came out of running it: a `scenarios:` key appended to a file that had none sat flush against the last rule, so `appendTo` now asks the encoder for a blank line above a top-level key it creates, which fixes the same thing for a first `bypass:` entry.
  - **The report is polled, not derived.** Retries, max retry wait and abandoned are the recorder's reading of its whole ring, and the browser keeps 500 events, so a count computed here would be a different number wearing the same name. The panel reads `GET /api/sessions/current/report` every second while it is on screen, which is the `useUpstreams` pattern, and the five tiles are the five figures `faultline run` prints so the two never disagree about one session.
  - **"Reset session" is the header's old Clear, renamed rather than duplicated.** Both are `DELETE /api/events`, and SPEC calls the thing a session, so the UI now says what the CLI and the docs say; it still goes through the stream hook's `clear()`, so the browser's list and the recorder cannot part ways. A row shows `(off)` beside a rule that is disabled, because a scenario's name and its rules can disagree - only one scenario is active and any rule can be switched by hand - and the new-scenario form opens ticked on whatever is enabled right now, which is the "save what you did" case without an endpoint that can only do that one thing. Editing and deleting a scenario stay the file's business: a delete has to answer what becomes of the rules written inside it.
- [x] **6.6 Config file awareness.** When a config file is in use, the UI shows its path and a "changes are saved to faultline.yaml" notice; when not, it shows "in-memory, use init to persist."
  - Verify (Manual): start once with and once without a config file and confirm the notice.
  - Passed 2026-09-11 against the built binary, driven through the browser: started in a directory with a `faultline.yaml` the header reads `faultline.yaml` and hovers as `changes are saved to /.../faultline.yaml`; started without one it reads `in-memory` and names `faultline init`. Spec in `docs/specs/2026-09-11-config-file-awareness.md`.
  - **The fact was already on the server with no way out of it.** `admin.Server` holds a `Persister` exactly when a file is in use, and the only implementation, `config.File`, already knew its path, so `GET /api/config` publishes `s.persist` rather than carrying a second copy of the same fact and `cmd/faultline/serve.go` is untouched. `Persister` gained `Path() string`: a persister that cannot say where it writes cannot answer the question a user asks before editing a rule.
  - **`persisted` is the answer, not an empty `path`.** A persister writing somewhere with no single path on disk would still not be holding the rules in memory, so the two are separate fields and the UI branches on the boolean. The path is resolved with `filepath.Abs` on the way out, because the banner prints what was typed into a terminal that knows where it is and this one is read in a browser, where `faultline.yaml` names nothing.
  - **Nothing is claimed before the answer arrives.** `useConfig` reads once - which file is in use is fixed at startup, its contents are what change, and the rule list already follows those over the stream - and until it returns, or if it fails, the header shows no chip at all rather than one asserting either state.
- [x] **6.7 Throughput and latency chart.** A rolling sixty second chart of requests per second and average latency over the live event stream, so a `delay` or a `refuse` is visible as a shape and not only as a number. `recharts`, ported from the predecessor's `MetricsChart`.
  - Verify (Manual): with traffic running, activate a delay and watch the latency line rise and the throughput line fall.
  - Passed 2026-09-11, confirmed by the human against the built binary: with the go-client example running, a delay raised the latency line and dropped the throughput line, and removing it returned both. One thing came back from that pass and is the behaviour rather than a fault in it: a delay long relative to the request rate leaves the latency line as separated dots, because a sequential client under a two second delay makes fewer than one request per second and a second with no request in it has no latency to connect. The dots are the seconds that do carry a measurement, and the throughput panel beside them is what says how sparse the traffic got.
  - **Sixty seconds is the axis here, not a smoothing window.** The predecessor kept one point per tick, each of them a sixty second trailing average, so switching a rule on arrived as a slow drift rather than an edge. This keeps one point per second and scrolls the window leftward, which is what makes a `delay` read as a step. The series is derived in the browser from the events the Live view already holds - no endpoint, nothing added to the socket, no server state - because a chart counting a different set of events than the rows under it would be a second answer to the same question. It follows the stream filters for that reason too, so filtering to one host isolates that host's shape. Spec in `docs/specs/2026-09-11-throughput-and-latency-chart.md`.
  - **An empty second has no latency, so it is a gap rather than a zero**, which is the one place the two panels differ: a second with no requests is genuinely `0` req/s but an unknown latency, and drawing it at zero would invent a dip. The browser keeps five hundred events, which at a high enough rate is less than a minute, so when that cap is reached the window starts at the oldest event still held rather than drawing seconds it cannot see as empty ones. Below the cap the whole minute is shown, because those empty seconds are real.
  - **Two things only the running binary showed.** A steady value drawn against an auto-scaled axis sat flush on the top edge and read as a clipped line, so the y domain keeps headroom above the data and zero as its floor. And the honest gaps broke the latency line into nothing at all in exactly the case the chart exists for: a delay worth injecting leaves whole seconds with no request in them, and a lone second between two gaps is a point, not a segment, so with no dot it drew nothing. The latency line marks each second it has a value; throughput has no gaps to mark and stays unmarked.
- [ ] **6.8 Design and UX pass.** Everything above was built one panel at a time and verified one panel at a time. This is the first task that looks at the product as a whole: walk every view and fix what is inconsistent, unexplained, or unpleasant to use.
      What that covers: one spacing scale, type scale and use of colour across every panel, so amber always means the same thing and a table row always sits the same way. Empty states that say what to do next rather than only what is missing. A loading state wherever something can be slow and an error state wherever something can fail, both of them in the panel that owns the thing rather than in a banner at the top. Focus rings and keyboard access on everything clickable, an accessible name on every icon-only control, and a tab order that follows the page. The window down to a laptop width without a horizontal scrollbar, and long values - a rule name, a header, a query string - truncating instead of stretching a column. Wording checked against the words SPEC.md uses, so the UI, the CLI and the docs call the same thing by the same name.
      Nothing here is a new capability. If a fix turns out to need an endpoint that does not exist or changes what a panel can do, that is a task of its own: write it on the later list and move on rather than widening this one.
  - Verify (Manual): walk SPEC.md's five-minute experience end to end in the browser, at a full window and at a laptop width, once on a fresh instance with no traffic and once with a few hundred events and several rules. Nothing on the way through should read as unfinished, and no step should need this document to get past.

**Stage 6 gate (Manual).** Repeat the five-minute experience from SPEC.md using only the browser after the `faultline run` command: see traffic, add delay, change to error, save as scenario, run the tests under it, read the report.

---

## Stage 7 — Agent interface

Goal: a coding agent can complete an inject, test, report cycle with no human involvement.

- [ ] **7.1 MCP server.** Using the official Go MCP SDK, expose tools over stdio (`faultline mcp`) and streamable HTTP at `/mcp` on the admin port. Tools: `list_upstreams`, `list_rules`, `add_rule`, `remove_rule`, `set_rule_enabled`, `list_scenarios`, `activate_scenario`, `deactivate_scenario`, `get_events` (with filters and limit), `wait_for_event` (filter plus timeout), `get_report`, `reset_session`. Each tool has a precise description and typed input schema derived from the fault registry.
  - Verify (Agent): tests with the SDK's in-memory transport calling every tool. Verify (Manual): add the server to Claude Code with `claude mcp add faultline -- faultline mcp` and ask it to list upstreams and add a delay rule; confirm the rule appears in the UI.
- [ ] **7.2 Standalone mode for agents.** `faultline mcp` without a running instance starts one in the background on default ports and stops it on exit, so an agent needs no separate step. Add a `start_wrapped` tool that runs a command under the proxy environment and returns its exit code and output tail.
  - Verify (Manual): with no Faultline running, ask the agent to run the Go example under a 503 rule and report retries. Expect a correct answer and a clean shutdown when the agent session ends.
- [ ] **7.3 Claude Code skill.** `skills/faultline/SKILL.md` teaching the agent when to reach for Faultline and three recipes: verify retry behavior, verify timeout configuration, and rehearse a dependency outage against a test suite. Include the expected shape of a good report to the user.
  - Verify (Manual): install the skill, say "check that examples/go-client handles httpbin being down," and expect the agent to run the recipe and produce a report without further prompting.
- [ ] **7.4 Thin client libraries.** Minimal Go and TypeScript clients (`clients/go`, `clients/ts`) for use inside tests: add rule, activate scenario, wait for event, get report. No business logic, just typed API calls.
  - Verify (Agent): each client has a test against a live Faultline started in the test. Verify (Manual): the Go example gains one integration test that uses the client to inject a `first_n` fault and asserts two retries occurred.

**Stage 7 gate (Manual).** In a fresh Claude Code session with the MCP server and skill installed, ask: "Verify the Spring Boot example retries on 503 from httpbin and tell me how long the user would have waited." Expect the agent to start Faultline, run the app, inject the fault, exercise it, and answer with numbers matching the UI report.

---

## Stage 8 — Containers

Goal: the container companion attach mode for Docker Compose, matching how Aevon runs services.

- [ ] **8.1 Docker image.** Multi-stage build producing a small image with the binary, listening on all interfaces, with the CA directory as a volume. Published to GHCR by CI on tags.
  - Verify (Manual): `docker run -p 9000:9000 -p 9001:9001 faultline` then `curl -x localhost:9001 http://httpbin.org/get` and see the event.
- [ ] **8.2 Compose companion example.** `examples/compose/` with Faultline plus the Spring Boot example, where the app service gets the proxy env vars and the CA mounted, and the JVM trust store built at startup by a tiny entrypoint helper shipped in the image (`faultline trust java`).
  - Verify (Manual): `docker compose up`, curl the app, see intercepted events in the UI at `localhost:9000`.
- [ ] **8.3 Transparent mode.** Faultline gains `--transparent`: with `NET_ADMIN`, it installs iptables rules in its own network namespace redirecting outbound 80 and 443 to itself and uses the original destination for routing. App services attach with `network_mode: "service:faultline"` and need no env vars for connection-tier faults. Response-tier faults still need the CA trusted in the app image.
  - Verify (Manual): second compose example using transparent mode with the Go example; no env vars on the app; events show tier `encrypted` until the CA is added, then `intercepted`.
- [ ] **8.4 Compose helper.** `faultline compose inject --service <name>` reads `docker-compose.yml` and writes `docker-compose.faultline.yml` override adding the companion and the settings for the named services, printing the `docker compose -f ... -f ...` command to run.
  - Verify (Manual): run it against `examples/compose` with the companion removed; the generated override reproduces the working setup.

**Stage 8 gate (Manual).** Take a real Aevon Compose file on a VM, run the helper against one Spring Boot service, bring it up, and rehearse a dependency outage from the UI. Expect intercepted events and a correct report, with no edits to the application image beyond trusting the CA.

---

## Stage 9 — Release

Goal: a stranger can install and use it.

- [ ] **9.1 Release pipeline.** GoReleaser builds Linux, macOS, Windows binaries for amd64 and arm64 on tags, publishes GitHub releases and the Docker image, and updates a Homebrew tap.
  - Verify (Manual): tag `v0.1.0-rc1`, watch the workflow, `brew install josipmusa/tap/faultline` on a Mac, run `faultline version`.
- [ ] **9.2 GitHub Action.** `josipmusa/setup-faultline` action installing the binary and optionally starting it, with an example workflow running the Go example's integration test.
  - Verify (Manual): the example workflow passes on a pull request in the repository.
- [ ] **9.3 Documentation.** README with the five-minute walkthrough and a GIF, `docs/` covering attach modes, trust setup per runtime, fault catalogue with examples, config reference generated from the schema, MCP tools reference, and troubleshooting.
  - Verify (Manual): hand the README to someone who has never seen the project and time them to first event.
- [ ] **9.4 Security review.** Confirm the admin API binds to localhost by default with an `--admin-bind` flag to widen it, the CA private key is created with owner-only permissions, event bodies can be disabled with `--no-bodies` for sensitive traffic, and the MCP HTTP endpoint respects the same bind rule. Document the threat model in `docs/security.md`.
  - Verify (Agent): tests for default bind and file permissions. Verify (Manual): read `docs/security.md` and confirm every claim matches behavior.
- [ ] **9.5 v0.1.0.** Tag, announce, open issues for the "later" list below.
  - Verify (Manual): release page shows all artifacts; a fresh install works on Linux and macOS.

**Stage 9 gate (Manual).** The full five-minute experience from SPEC.md on a machine that has never seen the source code, using only the README.

---

## Later, deliberately out of v0.1

- Shared instance identity via proxy credentials in `HTTPS_PROXY=http://service:token@faultline:9001`, with rules scoped per service and an admin token for the control surface.
- Prometheus metrics endpoint.
- Python client library.
- Kubernetes sidecar injection.
- gRPC, WebSocket, and raw TCP.
- Response recording for later replay (only if users ask; this is adjacent to mocking and out of scope by principle).
