spring.application.name=faultline-example-spring-boot
server.port=8080

# The dependency this app calls. Point it somewhere else to try Faultline
# against a real service.
example.upstream=https://httpbin.org
example.path=/get
